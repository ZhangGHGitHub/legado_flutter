# Final review package — book source manage wave 1
MERGE_BASE / WAVE1_START: 7076a755358db0c9873a0039e9fc0c30a6885382
HEAD: 064d78902d07a065f514b91386c1056f17fdde4f

## Commits
064d789 docs: record book source manage wave 1 parity b55af4a feat(sources): bottom bar explore, order, export, and share 46ebda3 feat(sources): row display name, explore dots, and overflow actions b1fab66 feat(sources): add sort dimensions and manual drag reorder a922ce6 fix(sources): move to top/bottom uses customOrder order 4f517f3 feat(sources): batch explore, order, export, and group APIs c3bee2b fix(explore): split explore URL vs enabled for source filters f74f1e3 feat(sources): persist BookSource order and explore fields

## Stat
 .superpowers/sdd/task-3-report.md                  |  115 ++  docs/UI_REPLICATION_PLAN.md                        |   18 +-  .../2026-07-20-book-source-manage-wave1-design.md  |  163 ++  lib/models/book_source.dart                        |   55 +  lib/pages/explore/explore_utils.dart               |   18 +-  lib/pages/sources/sources_page.dart                | 1947 ++++++++++++--------  lib/providers/source_order.dart                    |   35 +  lib/providers/source_provider.dart                 |  152 +-  test/models/book_source_engine_json_test.dart      |   32 +  test/pages/explore/explore_utils_test.dart         |   31 +  test/providers/source_order_logic_test.dart        |   57 +  11 files changed, 1813 insertions(+), 810 deletions(-)

## Diff
```
diff --git a/.superpowers/sdd/task-3-report.md b/.superpowers/sdd/task-3-report.md new file mode 100644 index 0000000..13e3cd4 --- /dev/null +++ b/.superpowers/sdd/task-3-report.md @@ -0,0 +1,115 @@ +# Task 3 Report: SourceProvider batch order / explore / export / group + +## Status + +DONE + +## What Was Implemented + +### Pure helpers (`lib/providers/source_order.dart`) + +| Function | Semantics | +|----------|-----------| +| `customOrdersAfterMoveToTop` | Selected sources first, stable relative order within each partition | +| `customOrdersAfterMoveToBottom` | Selected sources last, stable relative order within each partition | + +### SourceProvider APIs (`lib/providers/source_provider.dart`) + +| Method | Behavior | +|--------|----------| +| `setSourcesExploreEnabled` | `copyWith(enabledExplore:)` + `_dao.update` per match | +| `moveSourcesToTop` / `moveSourcesToBottom` | Compute order map via helpers 鈫?`_applyCustomOrders` | +| `reorderSources` | Assign `customOrder = index` for each URL in list | +| `exportSourcesJson` | `jsonEncode` of matching `toJson()` maps | +| `addGroupToSources` | Set `bookSourceGroup` + `SourceGroupCatalog.mergeFromSources` | +| `clearGroupOnSources` | Set `bookSourceGroup` to `''` | +| `_applyCustomOrders` | Shared persistence for order updates | + +`setSourcesGroup` now delegates to `addGroupToSources` (same persistence, no duplication). + +Share stays in UI via `Share.share` + `exportSourcesJson` (not in provider). + +## TDD Evidence + +### RED (Step 2) + +``` +flutter test test/providers/source_order_logic_test.dart +``` + +Result: **FAIL** 鈥?`source_order.dart` missing; `customOrdersAfterMoveToTop` / `customOrdersAfterMoveToBottom` not found. + +### GREEN (Step 4) + +``` +flutter test test/providers/source_order_logic_test.dart +dart analyze lib/providers/source_order.dart lib/providers/source_provider.dart +``` + +Result: **PASS** 鈥?`00:00 +2: All tests passed!`   +Analyzer: **No issues found!** + +Tests: + +1. `move to top reindexes selected first` +2. `move to bottom reindexes selected last` + +## Commit + +``` +4f517f3 feat(sources): batch explore, order, export, and group APIs +``` + +Files committed (only): + +- `lib/providers/source_order.dart` +- `lib/providers/source_provider.dart` +- `test/providers/source_order_logic_test.dart` + +## Out of Scope (per brief) + +- UI wiring (`Share.share`, batch action menus) 鈥?later tasks +- Provider integration tests for DAO persistence 鈥?pure order logic only in tests + +## Concerns / Follow-ups + +1. ~~**`moveSourcesToTop/Bottom` preserve list order from `_sources`**, not `customOrder` sort~~ 鈥?**Fixed** (see Review Fix below). +2. **`exportSourcesJson` order follows `_sources` iteration**, not input URL order 鈥?acceptable for share; reorder if UI needs explicit ordering. +3. **`reorderSources` only updates URLs present in the list** 鈥?sources not listed keep existing `customOrder` (brief specifies index per URL in order only). +4. No provider-level tests for explore/group/export 鈥?brief TDD scope is pure order helpers only. + +## Review Fix (Task 3) + +### Change + +- Added `sourcesInManualOrder` in `lib/providers/source_order.dart` 鈥?sorts by `customOrder` asc, tie-break `bookSourceUrl`. +- `moveSourcesToTop` / `moveSourcesToBottom` now pass `sourcesInManualOrder(_sources)` to order helpers instead of raw DAO list order. +- Added shuffled-list tests documenting correct behavior when list order disagrees with `customOrder`. + +### Verification + +``` +flutter test test/providers/source_order_logic_test.dart +``` + +Result: **PASS** 鈥?`00:00 +4: All tests passed!` + +Tests: + +1. `move to top reindexes selected first` +2. `move to bottom reindexes selected last` +3. `move to top uses customOrder when list is shuffled` +4. `move to bottom uses customOrder when list is shuffled` + +### Commit + +``` +fix(sources): move to top/bottom uses customOrder order +``` + +Files committed (only): + +- `lib/providers/source_order.dart` +- `lib/providers/source_provider.dart` +- `test/providers/source_order_logic_test.dart` +- `.superpowers/sdd/task-3-report.md` diff --git a/docs/UI_REPLICATION_PLAN.md b/docs/UI_REPLICATION_PLAN.md index 1950787..27ee2c0 100644 --- a/docs/UI_REPLICATION_PLAN.md +++ b/docs/UI_REPLICATION_PLAN.md @@ -367,17 +367,17 @@  ### 2.8 涔︽簮绠＄悊 (`ui/book/source/manage/` 鈿?`sources_page.dart`)    | 瀵圭収椤?| Jingshiro | Flutter 鐜扮姸 | 宸窛 |  |--------|-----------|-------------|------|  | 椤舵爮鎼滅储 | TitleBar + `view_search` hint銆屾悳绱功婧愩€?| 鉁?涓昏壊椤舵爮鍐呭祵鎼滅储锛?026-07-17锛?| | -| 鎺掑簭鑿滃崟 | 闄嶅簭 + 鎵嬪姩/鍚嶇О/URL/鍚敤鈥?| 鉁?闄嶅簭/鎵嬪姩/鍚嶇О/URL/鍚敤 | respondTime/lastUpdateTime 浠嶅紑鏀?| +| 鎺掑簭鑿滃崟 | 闄嶅簭 + 鎵嬪姩/鏅鸿兘/鍚嶇О/URL/鍚敤/鏇存柊鏃堕棿/鍝嶅簲鏃堕棿 | 鉁?Wave 1锛?026-07-21锛夛細鍏ㄧ淮搴?+ 鎵嬪姩鎷栨嫿 | 鍩熷悕鍒嗙粍妯″紡涓嬬鐢ㄦ嫋鎷?|  | 鍒嗙粍鑿滃崟 | 鍒嗙粍绠＄悊/鍚敤绂佺敤/鐧诲綍/鏈垎缁?鍙戠幇寮€闂?鍒嗙粍鍚?| 鉁?绛涢€夐」瀵归綈锛涘垎缁勭鐞?Dialog 鍗犱綅 | | -| 鏇村鑿滃崟 | 鏂板缓/鏈湴/缃戠粶/浜岀淮鐮?鎸夊煙鍚嶅垎缁?甯姪 | 鉁?+ 绮樿创/甯傚満/鏍￠獙宸插惎鐢?| 甯姪鍗犱綅 | -| 鍒楄〃琛?| 鍕鹃€?鍚?\| Switch \| 缂栬緫 \| 鈰?\| 鍙戠幇缁跨偣 | 鉁?`item_book_source` 瀵归綈 | | +| 鏇村鑿滃崟 | 鏂板缓/鏈湴/缃戠粶/浜岀淮鐮?鎸夊煙鍚嶅垎缁?甯姪 | 鉁?瀵归綈 Jingshiro锛?*涓嶅惈**绮樿创/甯傚満/鏍￠獙鈥斺€旂鐞嗛〉婧㈠嚭鏃犳涓夐」锛?| 甯姪鍗犱綅 | +| 鍒楄〃琛?| 鍕鹃€?鍚?鍒嗙粍) \| Switch \| 缂栬緫 \| 鈰?\| 鍙戠幇缁?绾㈢偣 | 鉁?Wave 1锛歚鍚嶇О (鍒嗙粍)` + 鍙戠幇鍙岃壊鐐?+ 琛?鈰?缃《/鎼滅储/璋冭瘯/鍙戠幇 | |  | 缁?绾?鐏扮偣 | 鏍￠獙鐘舵€?| 鉁?涔﹀悕鏃?`SourceStatusDot` | |  | 搴曟爮 | SelectActionBar 鍏ㄩ€?鍙嶉€?鍒犻櫎/鈰?| 鉁?甯搁┗锛?026-07-17锛?| | -| 鎵归噺鎿嶄綔 | 鍚敤/绂佺敤/鍒嗙粍/鏍￠獙/鍒犻櫎 | 鉁?| | +| 鎵归噺鎿嶄綔 | 鍚敤/绂佺敤/鍙戠幇/鍒嗙粍/缃《/瀵煎嚭/鍒嗕韩/鏍￠獙/鍒犻櫎 | 鉁?Wave 1 搴曟爮銆屾洿澶氥€嶈ˉ榻?| 娣诲姞/绉婚櫎鍒嗙粍涓哄崟鍒嗙粍璇箟锛圵ave 2 閫楀彿澶氭爣绛撅級 |  | 瀵煎叆鏂瑰紡 | JSON/URL/浜岀淮鐮?鍓创鏉?| 鉁?| |    ---    ## 涓夈€佺己澶遍〉闈㈣缁嗗紑鍙戜换鍔?@@ -443,16 +443,20 @@  - [x] 绮惧噯鎼滅储锛堜功鍚嶅寘鍚?+ 鍙€変綔鑰呰繃婊わ級  - [x] 鎼滅储鑼冨洿锛堝叏閮?/ 鎸夊垎缁?/ 鑷€変功婧愶級  - [x] 搴曟爮銆屽叡 N 鏈?路 M 涓功婧愩€嶏紙鎼滅储涓疄鏃舵洿鏂帮級  - [ ] 涔︽簮鏍囩绛涢€夛紙鍚庣画鍙姞锛?  -#### Task UI-4: 涔︽簮绠＄悊瀵归綈 (`sources_page.dart`) 鈥?鉁?鍩烘湰瀹屾垚锛?026-07-14锛?+#### Task UI-4: 涔︽簮绠＄悊瀵归綈 (`sources_page.dart`) 鈥?馃煛 Wave 1 瀹屾垚锛?026-07-21锛夛紱Wave 2 浠嶅紑鏀?   - [x] 鎸?`bookSourceGroup` 鍒嗙粍灞曠ず锛坄SectionHeader` 鍒嗙粍鍒楄〃锛? - [x] 鎵归噺鎿嶄綔鏍忥紙澶氶€夛細鍚敤/绂佺敤/鍒嗙粍/鏍￠獙/鍒犻櫎锛? - [x] 鍒嗙粍鏍囬 + 缁?绾?鐏扮偣楠岃瘉锛堥€氳繃/澶辫触/鏈牎楠岋級  - [x] 涔︽簮鎼滅储/鎺掑簭锛堝悕绉?鍒嗙粍/鍚敤锛?+- [x] **Wave 1**锛歚customOrder`/`weight`/`lastUpdateTime`/`respondTime`/`enabledExplore` 妯″瀷 + 鏅鸿兘/鏇存柊鏃堕棿/鍝嶅簲鏃堕棿鎺掑簭 + 鎵嬪姩鎷栨嫿 +- [x] **Wave 1**锛氳鏍囬 `鍚嶇О (鍒嗙粍)`銆佸彂鐜扮豢/绾㈢偣銆佽 鈰紙缃《/缃簳/鎼滅储/璋冭瘯/鍚敤路绂佺敤鍙戠幇锛?+- [x] **Wave 1**锛氬簳鏍忔洿澶氾紙鍚敤路绂佺敤鍙戠幇銆佹坊鍔犅风Щ闄ゅ垎缁勩€佺疆椤堵风疆搴曟墍閫夈€佸鍑郝峰垎浜功婧愶級 +- [ ] **Wave 2**锛氬鍏ラ瑙?Dialog銆佹牎楠屽叧閿瘝+璁剧疆銆侀€楀彿澶氬垎缁勩€佸府鍔╅〉銆佹粦鍔ㄥ閫夈€佽鍐呮牎楠?debug 鏂囨    #### Task UI-5: 鐩綍椤靛寮?(`toc_sheet.dart`) 鈥?鉁?瀵归綈鎴浘锛?026-07-15锛?   - [x] 鍏ㄩ〉璺敱锛堥槄璇诲櫒 / 涔︾睄璇︽儏鍏辩敤 `TocSheet.show`锛? - [x] AppBar锛氳繑鍥?| 銆岀洰褰曘€嶃€屼功绛俱€峊ab锛堜富鑹蹭笅鍒掔嚎锛墊 鎼滅储 | 婧㈠嚭鑿滃崟锛堟搴?鍊掑簭銆佸畾浣嶅綋鍓嶏級 @@ -667,11 +671,11 @@    ```  Task UI-1:  闃呰鍣ㄥ簳鏍?+ 椤舵爮鑷姩闅愯棌 + 鏇村鑿滃崟  鉁?鍩烘湰瀹屾垚锛?026-07-14锛? Task UI-2:  闃呰鍣ㄨ缃ˉ鍏紙瀛椾綋/TTS/鑷姩闃呰/鐐瑰嚮琛屼负/缈婚〉閿級  馃煛 瓒呮椂鍒嗘。+缈婚〉浜旀。+鍏ㄦ枃鎼滅储+妯℃嫙杩借+涓婚 zip锛汬TTP TTS/鐪熶豢鐪?璇嶇骇绠€绻?FontLoader 浠嶅紑鏀? Task UI-3:  鎼滅储椤垫寜涔︽簮鍒嗙粍 + 绮惧噯鎼滅储 + 鎼滅储鑼冨洿  鉁?鍩烘湰瀹屾垚锛?026-07-14锛?-Task UI-4:  涔︽簮绠＄悊鍒嗙粍灞曠ず + 鎵归噺鎿嶄綔 + 缁跨孩鐏扮偣楠岃瘉  鉁?鍩烘湰瀹屾垚锛?026-07-14锛?+Task UI-4:  涔︽簮绠＄悊 Wave 1锛堟帓搴?琛?搴曟爮锛? 馃煛 Wave 1 瀹屾垚锛?026-07-21锛夛紱Wave 2 浠嶅紑鏀? Task UI-5:  鐩綍椤靛榻愭埅鍥撅紙AppBar/瀛楁暟路浜戞爣/搴曟爮锛? 鉁?瀵归綈鎴浘锛?026-07-15锛? Task UI-6:  涔︾睄璇︽儏瀵归綈 Jingshiro 鎴浘锛堣嫳闆勫尯/鍏冩暟鎹孩鑺墖/搴曟爮锛? 鉁咃紙2026-07-15锛? Task UI-7:  涔︽灦闀挎寜鑿滃崟瀹屾暣鍖? 鉁?鍩烘湰瀹屾垚锛?026-07-14锛? Task UI-8:  鎴戠殑椤佃彍鍗曡ˉ榻?+ 蹇嵎鍥涙牸琛屼负  鉁?鍩烘湰瀹屾垚锛?026-07-14锛? ``` @@ -736,11 +740,11 @@ Task UI-12:  RSS 鏂囩珷鍒楄〃 + 闃呰  鉁?澶嶇敤 Jingshiro Rss/ParserByRule/De  | 鎼滅储 | 90% | UI-3锛氭寜涔︽簮鍒嗙粍 + 绮惧噯鎼滅储 + Scope |  | 鍙戠幇 | 85% | 鎺ヨ繎瀹屾垚 |  | 涔︾睄璇︽儏 | 96% | UI-6锛氭埅鍥惧竷灞€锛堟ā绯婂ご鍥?绾㈣姱鐗?鍒犻櫎+闃呰搴曟爮锛夛紱鎹㈡簮椤靛悗绔粛鍗犱綅锛涘浘鏍囧瓧褰㈠彲鍐嶆姞 |  | 鐩綍 | 95% | UI-5锛氬叏椤?AppBar+瀛楁暟/浜戞爣+搴曟爮锛涙孩鍑鸿彍鍗曟绾ч」浠嶅彲鍐嶆姞 |  | **闃呰鍣?* | **93%** | UI-1+UI-2锛氳秴鏃跺垎妗?缈婚〉浜旀。+娌夋蹈+绯荤粺 TTS+鍏ㄦ枃鎼滅储(缂撳瓨)+妯℃嫙杩借+涓婚 zip锛汬TTP TTS/璇嶇骇绠€绻?鍏ㄤ功鑱旂綉鎼滀粛寮€鏀?| -| 涔︽簮绠＄悊 | 97% | UI-4 chrome 2026-07-17 瀵归綈 Jingshiro锛堟悳绱㈤《鏍?琛屽竷灞€/SelectActionBar锛夛紱鍒嗙粍绠＄悊 Dialog銆侀儴鍒嗘帓搴忕淮搴︿粛寮€鏀?| +| 涔︽簮绠＄悊 | 97% | UI-4 Wave 1锛?026-07-21锛夛細鎺掑簭/鎷栨嫿/琛岃彍鍗?搴曟爮鏇村锛沇ave 2锛堝鍏ラ瑙?鏍￠獙璁剧疆/澶氬垎缁?甯姪锛変粛寮€鏀?|  | RSS | 92% | UI-12锛氬垪琛?闃呰+sortUrl Tab+鏀惰棌+婧愮紪杈戯紱璋冭瘯浠嶅紑鏀?|  | 鏂版ā鍧?鏈夊０/婕敾/鎵爜绛? | 40% | UI-17 鎵爜鍩烘湰瀹屾垚锛沀I-22/23/25 宸?1:1 chrome 杩斿伐锛圡P3/鐪熸极鐢绘簮/搴楅摵浠嶅紑鏀撅級 |  | **缁煎悎** | **~60%** | |    --- diff --git a/docs/superpowers/specs/2026-07-20-book-source-manage-wave1-design.md b/docs/superpowers/specs/2026-07-20-book-source-manage-wave1-design.md new file mode 100644 index 0000000..8183d8f --- /dev/null +++ b/docs/superpowers/specs/2026-07-20-book-source-manage-wave1-design.md @@ -0,0 +1,163 @@ +# 涔︽簮绠＄悊瀵归綈 鈥?绗竴娉㈣璁?+ +> 鏃ユ湡锛?026-07-20   +> 鐘舵€侊細**绗竴娉㈠凡瀹炵幇**锛?026-07-21锛夛紱瀹炵幇璁″垝瑙?`docs/superpowers/plans/2026-07-20-book-source-manage-wave1.md`   +  +> 瀵圭収锛欽ingshiro `BookSourceActivity` + `BookSourceAdapter` + `menu/book_source*.xml` + `item_book_source.xml` + SelectActionBar `book_source_sel.xml`   +> 瀹炵幇闈細`lib/pages/sources/sources_page.dart`銆乣lib/models/book_source.dart`銆乣lib/providers/source_provider.dart`銆佺浉鍏?DAO/store + +## 1. 鐩爣涓庤寖鍥?+ +### 1.1 鐩爣 + +瀵归綈 Jingshiro 涔︽簮绠＄悊椤电殑**鏃ュ父绠＄悊**鑳藉姏锛氭墜鍔ㄥ簭涓庢帓搴忕淮搴︺€佸垪琛ㄨ灞曠ず涓庤鑿滃崟銆佸簳鏍忔壒閲忋€屾洿澶氥€嶆搷浣溿€傝涓轰笌鏂囨浠?Jingshiro 涓哄噯锛涘竷灞€娌跨敤鐜版湁 Flutter chrome锛堟悳绱㈤《鏍?+ SelectActionBar锛夈€?+ +### 1.2 鏈尝鍖呭惈锛圥0/P1锛?+ +1. **妯″瀷涓€绛夊瓧娈?*锛歚customOrder`銆乣lastUpdateTime`銆乣weight`銆乣enabledExplore`锛沗respondTime` 鍙鍙帓搴忥紙鏍￠獙鍐欏洖鍙浜屾尝锛夈€?+2. **鎺掑簭鑿滃崟**锛氱幇鏈夐檷搴?鎵嬪姩/鍚嶇О/URL/鍚敤 + **鏅鸿兘鎺掑簭**銆?*鏇存柊鏃堕棿鎺掑簭**锛涳紙鑻ュ瓧娈靛氨缁級**鍝嶅簲鏃堕棿鎺掑簭**銆?+3. **鎵嬪姩鎺掑簭**锛氫粎銆屾墜鍔ㄣ€嶆ā寮忎笅鍙嫋鎷斤紱缃《/缃簳鍐?`customOrder`锛涢噸鍚悗椤哄簭淇濇寔銆?+4. **鍒楄〃琛?*锛氭爣棰?`鍚嶇О (鍒嗙粍)`锛涘彂鐜扮偣缁?绾紙鏃?exploreUrl 涓嶆樉绀猴級銆?+5. **琛?鈰?*锛氱疆椤躲€佺疆搴曪紙浠呮墜鍔ㄦ帓搴忓彲瑙侊級銆佹悳绱紙杩涙悳绱㈤〉骞堕檺瀹氳涔︽簮锛夈€佽皟璇曘€佸惎鐢ㄥ彂鐜般€佺鐢ㄥ彂鐜帮紱淇濈暀缂栬緫/鏍￠獙/鐧诲綍/鍚敤绂佺敤/鍒犻櫎銆?+6. **搴曟爮鏇村**锛氬惎鐢?绂佺敤鍙戠幇銆佺疆椤?缃簳鎵€閫夈€佸鍑烘墍閫夈€佸垎浜功婧愩€佹坊鍔犲垎缁勩€佺Щ闄ゅ垎缁勶紱淇濈暀鍚敤/绂佺敤鎵€閫夈€佽缃垎缁勩€佹牎楠屾墍閫夈€?+ +### 1.3 鏈尝涓嶅仛锛堢浜屾尝+锛?+ +- 瀵煎叆棰勮 Dialog锛圛mportBookSourceDialog锛?+- 鏍￠獙鍏抽敭璇嶅脊绐?+ 鏍￠獙璁剧疆椤?+- 閫楀彿澶氬垎缁勫畬鏁磋涔夛紙`A,B,C` 鐨?add/remove 鏍囩璇箟锛?+- 甯姪椤?/ 棣栨杩涘叆鑷姩甯姪 +- 婊戝姩澶氶€夈€佹牎楠岃鍐呴€愭 debug 鏂囨 +- 缃戠粶瀵煎叆 URL 鍘嗗彶 +- 鏂囨。涓鏍囩殑 AppBar銆岀矘璐?甯傚満/鏍￠獙宸插惎鐢ㄣ€嶏紙绠＄悊椤垫孩鍑鸿彍鍗曚笉鎭㈠杩欎笁椤癸級 + +### 1.4 闈炵洰鏍?+ +- 涓嶉噸鍋氫功婧愮紪杈戝櫒 / 鐧诲綍椤?+- 涓嶆敼瑙勫垯寮曟搸鎴栨悳绱㈠紩鎿庢湰韬紙浠呮墦寮€鎼滅储椤靛苟浼犲叆婧愰檺瀹氾級 +- 涓嶅紩鍏ユ柊鐨勮繙绋嬨€屼功婧愬競鍦恒€嶅崗璁?+ +--- + +## 2. 鏁版嵁妯″瀷涓庢寔涔呭寲 + +### 2.1 `BookSource` 鏂板瀛楁 + +| 瀛楁 | 绫诲瀷 | 榛樿 | 璇存槑 | +|------|------|------|------| +| `customOrder` | `int` | `0` | 鎵嬪姩搴忥紱缃《=褰撳墠鏈€灏忊垝1 鎴栭噸缂栧彿锛涙嫋鎷藉悗鎸夊垪琛ㄤ笅鏍囬噸鍐?| +| `lastUpdateTime` | `int` | `0` | 姣鏃堕棿鎴筹紱瀵煎叆/缂栬緫淇濆瓨鏃跺彲鏇存柊 | +| `weight` | `int` | `0` | 鏅鸿兘鎺掑簭锛涙潵鑷?JSON锛屾湰娉笉鑷姩鏀瑰啓 | +| `enabledExplore` | `bool` | `true` | 鍙戠幇寮€鍏筹紱涓?`exploreUrl` 閰嶅悎 UI | +| `respondTime` | `int` | `180000` | 鍙 + 鎺掑簭锛涙牎楠屾垚鍔熷啓鍥炵暀缁欑浜屾尝 | + +### 2.2 JSON / 瀛樺偍 + +- `fromJson` / `toJson` / `copyWith` / `rawSourceJson` 鍚屾璇诲啓涓婅堪瀛楁锛堜笌 legado JSON 閿悕涓€鑷达級銆?+- 宸叉湁涔︽簮锛氫粠 `rawSourceJson` 鍥炲～锛涚己鐪佺敤涓婅〃榛樿鍊笺€?+- DAO / SharedPreferences锛堟垨鐜版湁 source store锛夊繀椤绘寔涔呭寲杩欎簺瀛楁锛?*绂佹**鍙椿鍦ㄥ唴瀛樸€?+ +### 2.3 `SourceProvider` API锛堟湰娉級 + +| 鏂规硶 | 琛屼负 | +|------|------| +| `setSourcesExploreEnabled(urls, bool)` | 鎵归噺鏀?`enabledExplore` | +| `moveSourcesToTop(urls)` / `moveSourcesToBottom(urls)` | 璋冩暣 `customOrder` 骞朵繚瀛橈紱鍒楄〃鍒锋柊 | +| `reorderSources(orderedUrls)` | 鎸夊彲瑙佸垪琛ㄩ『搴忛噸缂栧彿 `customOrder`锛堟墜鍔ㄦ帓搴忔嫋鎷藉悗锛?| +| `exportSources(urls)` | 杩斿洖/鍐欏嚭鎵€閫夋簮 JSON 鏁扮粍锛堟枃浠朵繚瀛橈級 | +| `shareSources(urls)` | `share_plus` 鍒嗕韩 JSON 鏂囨湰鎴栦复鏃舵枃浠?| +| `addGroupToSources(urls, group)` / `clearGroupOnSources(urls)` | 鏈尝锛?*鍗曞垎缁勫瓧绗︿覆**鈥斺€旀坊鍔?璁句负璇ュ垎缁勫悕锛涚Щ闄?娓呯┖ `bookSourceGroup`锛堜笉鍋氶€楀彿澶氭爣绛撅級 | + +鐜版湁 `setSourcesEnabled`銆乣setSourcesGroup`锛堟暣涓叉浛鎹級銆乣deleteSources`銆佹牎楠屾祦绋嬩繚鎸佷笉鍙樸€?+ +--- + +## 3. UI 琛屼负 + +### 3.1 椤舵爮鎺掑簭 + +- 鑿滃崟椤癸紙涓枃锛夛細闄嶅簭锛堝紑鍏筹級銆佹墜鍔ㄦ帓搴忋€佹櫤鑳芥帓搴忋€佸悕绉般€乁RL銆佸惎鐢ㄧ姸鎬併€佹洿鏂版椂闂淬€侊紙鍙€夛級鍝嶅簲鏃堕棿銆?+- **鎵嬪姩**锛氬垪琛ㄩ『搴?= `customOrder` 鍗囧簭锛堥檷搴忓紑鍏冲垯鍙嶈浆锛夛紱鍚敤 `ReorderableListView`锛堟垨绛変环锛夈€?+- **鏅鸿兘**锛歚weight`锛?*鏇存柊鏃堕棿**锛歚lastUpdateTime`锛?*鍝嶅簲鏃堕棿**锛歚respondTime`锛涘叾浣欏悓鐜伴€昏緫銆?+- 闈炴墜鍔ㄦā寮忥細闅愯棌琛屻€岀疆椤?缃簳銆嶏紱绂佺敤鎷栨嫿銆?+ +### 3.2 鍒楄〃琛?+ +- 鏍囬锛歚bookSourceGroup.trim().isEmpty ? name : '$name ($group)'`銆?+- 鍙戠幇鐐癸細瀛樺湪鏈夋晥 explore URL 鏃舵樉绀猴紱`enabledExplore` 鈫?缁匡紝鍚﹀垯绾紙瀵归綈 Jingshiro 鍙岃壊鐐癸紝鏇挎崲褰撳墠銆屼粎缁跨偣銆嶏級銆?+- 琛屽竷灞€楠ㄦ灦锛堝嬀閫?/ Switch / 缂栬緫 / 鈰級淇濇寔鐜版湁瀵归綈锛屼笉閲嶅仛瑙嗚涓婚銆?+ +### 3.3 琛岃彍鍗?+ +| 椤?| 鏉′欢 / 琛屼负 | +|----|-------------| +| 缃《 / 缃簳 | 浠?`_sort == manual` | +| 鎼滅储 | `Navigator` 鈫?`SearchPage`锛堟垨鐜版湁鎼滅储鍏ュ彛锛夛紝闄愬畾鍗曚釜 `bookSourceUrl` | +| 璋冭瘯 | 鎵撳紑宸叉湁 `source_debug_page.dart` | +| 鍚敤鍙戠幇 / 绂佺敤鍙戠幇 | 鍐?`enabledExplore` | +| 缂栬緫 / 鏍￠獙 / 鐧诲綍 / 鍚敤绂佺敤 / 鍒犻櫎 | 淇濇寔鐜版湁 | + +### 3.4 搴曟爮銆屾洿澶氥€?+ +椤哄簭寤鸿瀵归綈 Jingshiro `book_source_sel`锛堝彲寰皟锛屼絾鏂囨涓€鑷达級锛?+ +1. 鍚敤鎵€閫?/ 绂佺敤鎵€閫夛紙宸叉湁锛?+2. 鍚敤鍙戠幇 / 绂佺敤鍙戠幇 +3. 璁剧疆鍒嗙粍锛堝凡鏈夛紝鏁翠覆鏇挎崲锛?+4. 娣诲姞鍒嗙粍 / 绉婚櫎鍒嗙粍锛堟湰娉㈠崟鍒嗙粍璇箟锛岃 2.3锛?+5. 缃《鎵€閫?/ 缃簳鎵€閫?+6. 瀵煎嚭鎵€閫?/ 鍒嗕韩涔︽簮 +7. 鏍￠獙鎵€閫夛紙宸叉湁锛?+ +鏃犻€変腑鏃讹細涓庣幇閫昏緫涓€鑷粹€斺€旀彁绀恒€岃鍏堥€夋嫨涔︽簮銆嶆垨绂佺敤鍏ュ彛锛堜紭鍏堜笌鐜板簳鏍忋€屽垹闄ゃ€嶇伆鎬佷竴鑷达級銆?+ +瀵煎嚭锛氱郴缁熸枃浠朵繚瀛樻垨搴旂敤瀵煎嚭鐩綍锛屾垚鍔?SnackBar銆?  +鍒嗕韩锛氱郴缁熷垎浜潰鏉匡紱澶辫触 SnackBar銆?+ +### 3.5 鍙戠幇绛涢€変慨姝?+ +`explore_on` / `explore_off` 浠?**鏄惁鏈?exploreUrl + `enabledExplore`** 涓哄噯锛?*涓?*鍐嶅己鍒惰姹?`enabled == true`锛堝榻?Jingshiro `enabledExplore` 鏌ヨ璇箟锛夈€傚伐鍏峰嚱鏁?`sourceHasExplore` 鑻ユ贩鍏?enabled锛屾湰娉㈡媶鎴愩€屾湁鍙戠幇 URL銆嶄笌銆屽彂鐜板凡鍚敤銆嶄袱涓垽鏂€?+ +--- + +## 4. 鏂囦欢褰卞搷锛堥鏈燂級 + +| 鏂囦欢 | 鍙樻洿 | +|------|------| +| `lib/models/book_source.dart` | 鏂板瓧娈?+ JSON | +| Source DAO / store锛堢幇鏈夎矾寰勶級 | 鎸佷箙鍖栧瓧娈?| +| `lib/providers/source_provider.dart` | 鎵归噺 API | +| `lib/pages/sources/sources_page.dart` | 鎺掑簭/鎷栨嫿/琛岃彍鍗?搴曟爮 | +| `lib/pages/explore/explore_utils.dart` | 鍙戠幇鍒ゆ柇鎷嗗垎 | +| `lib/widgets/...` 鎴栬鍐呯粍浠?| 鍙戠幇鍙岃壊鐐癸紙鍙敼 `SourceStatusDot` 鏃佺嫭绔嬬偣锛?| +| 鎼滅储椤靛叆鍙?| 鏀寔銆屽崟婧愰檺瀹氥€嶅弬鏁帮紙鑻ュ皻鏃狅級 | +| `docs/UI_REPLICATION_PLAN.md` 搂2.8 | 鏈尝瀹屾垚鍚庝慨姝ｈ繃鏃?鉁?鎻忚堪 | + +娴嬭瘯锛氭ā鍨?JSON round-trip锛汸rovider 缃《/鎷栨嫿椤哄簭锛涘彲閫?widget 鍐掔儫锛堟湁鍒欒ˉ锛夈€?+ +--- + +## 5. 楠屾敹鏍囧噯 + +1. 鎵嬪姩鎺掑簭鎷栨嫿鍚庢潃杩涚▼閲嶅惎锛岄『搴忎笉鍙樸€?+2. 琛?鎵归噺缃《銆佺疆搴曞悗 `customOrder` 涓庡垪琛ㄤ竴鑷淬€?+3. 鏈?exploreUrl 鐨勬簮锛氬惎鐢ㄥ彂鐜?缁跨偣锛岀鐢?绾㈢偣锛涚瓫閫夈€屽惎鐢ㄥ彂鐜?绂佺敤鍙戠幇銆嶇粨鏋滄纭€?+4. 鏍囬鏄剧ず `鍚嶇О (鍒嗙粍)`銆?+5. 瀵煎嚭鎵€閫夊緱鍒板悎娉曚功婧?JSON 鏁扮粍锛涘垎浜兘璋冭捣绯荤粺闈㈡澘銆?+6. 鏅鸿兘/鏇存柊鏃堕棿锛堝強鍝嶅簲鏃堕棿鑻ュ惎鐢級鎺掑簭缁撴灉绗﹀悎瀛楁鏂瑰悜 + 闄嶅簭寮€鍏炽€?+7. 鐜版湁锛氬鍏ャ€佸垹闄ゃ€佹牎楠屻€佸垎缁勭鐞?Dialog銆佺綉缁?鏈湴/浜岀淮鐮佸鍏?鈥?**鏃犲洖褰?*銆?+ +--- + +## 6. 椋庨櫓涓庝緷璧?+ +- **鎼滅储鍗曟簮闄愬畾**锛氳嫢 `SearchPage` 鏃?scope API锛屾湰娉㈤渶鏈€灏忓弬鏁版墿灞曪紙P0 for 琛屻€屾悳绱€嶏級銆?+- **澶у垪琛ㄦ嫋鎷芥€ц兘**锛氭部鐢?`ReorderableListView.builder`锛涘煙鍚嶅垎缁勬ā寮忎笅鎷栨嫿浠呭湪銆岄潪鍩熷悕鍒嗙粍銆嶆垨浠?flat 鍒楄〃鍚敤锛堟帹鑽愶細`_groupByDomain == true` 鏃剁鐢ㄦ嫋鎷藉苟 SnackBar 鎻愮ず鍏堝叧鍩熷悕鍒嗙粍锛夈€?+- **鍗曞垎缁?vs 澶氬垎缁?*锛氭坊鍔?绉婚櫎鍒嗙粍鏂囨涓?Jingshiro 鎺ヨ繎锛屼絾璇箟绠€鍖栵紱绗簩娉㈠啀鍋氶€楀彿鏍囩锛岄伩鍏嶆湰娉㈠崐鍚婂瓙澶氭爣绛俱€?+ +--- + +## 7. 鍚庣画娉㈡锛堝蹇橈級 + +- **绗簩娉?*锛氬鍏ラ瑙堛€佹牎楠屽叧閿瘝+璁剧疆銆佸鍒嗙粍閫楀彿鏍囩銆佹牎楠岀粨鏋滄寔涔呭寲銆?+- **绗笁娉?*锛氬府鍔┿€佹粦鍔ㄥ閫夈€佽鍐呮牎楠?debug 鏂囨銆乁RL 瀵煎叆鍘嗗彶銆佹枃妗ｆ暣浣撶櫨鍒嗘瘮鍥炲啓銆?diff --git a/lib/models/book_source.dart b/lib/models/book_source.dart index e7940e3..583f362 100644 --- a/lib/models/book_source.dart +++ b/lib/models/book_source.dart @@ -6,10 +6,17 @@ class BookSource {    final String bookSourceName;    final String bookSourceType;    final String bookSourceGroup;    final bool enabled;   +  // 鈹€鈹€ 鎺掑簭 / 鍙戠幇 鈹€鈹€ +  final int customOrder; +  final int lastUpdateTime; +  final int weight; +  final bool enabledExplore; +  final int respondTime; +    // 鈹€鈹€ 鎼滅储瑙勫垯 鈹€鈹€    final String ruleSearchUrl;    final String ruleSearchList;    final String ruleSearchName;    final String ruleSearchAuthor; @@ -51,10 +58,15 @@ class BookSource {      required this.bookSourceUrl,      required this.bookSourceName,      this.bookSourceType = '0',      this.bookSourceGroup = '',      this.enabled = true, +    this.customOrder = 0, +    this.lastUpdateTime = 0, +    this.weight = 0, +    this.enabledExplore = true, +    this.respondTime = 180000,      this.ruleSearchUrl = '',      this.ruleSearchList = '',      this.ruleSearchName = '',      this.ruleSearchAuthor = '',      this.ruleSearchCoverUrl = '', @@ -240,16 +252,29 @@ class BookSource {      return '';    }      factory BookSource.fromJson(Map<String, dynamic> json) {      String? safeString(dynamic v) => (v is String) && v.isNotEmpty ? v : null; +    int safeInt(dynamic v, [int d = 0]) { +      if (v is int) return v; +      if (v is num) return v.toInt(); +      return d; +    } +      bool safeBool(dynamic v) {        if (v is bool) return v;        if (v is int) return v != 0;        return true;      }   +    bool safeEnabledExplore(dynamic v) { +      if (v == null) return true; +      if (v is bool) return v; +      if (v is int) return v != 0; +      return true; +    } +      // Legado 鍏煎: searchUrl 鈫?ruleSearchUrl      final resolvedSearchUrl =          safeString(json['ruleSearchUrl']) ??          safeString(json['searchUrl']) ??          ''; @@ -277,10 +302,15 @@ class BookSource {        bookSourceUrl: safeString(json['bookSourceUrl']) ?? '',        bookSourceName: safeString(json['bookSourceName']) ?? '鏈懡鍚嶄功婧?,        bookSourceType: safeString(json['bookSourceType']) ?? '0',        bookSourceGroup: safeString(json['bookSourceGroup']) ?? '',        enabled: safeBool(json['enabled']), +      customOrder: safeInt(json['customOrder']), +      lastUpdateTime: safeInt(json['lastUpdateTime']), +      weight: safeInt(json['weight']), +      enabledExplore: safeEnabledExplore(json['enabledExplore']), +      respondTime: safeInt(json['respondTime'], 180000),        ruleSearchUrl: resolvedSearchUrl,        ruleSearchList:            nested('ruleSearch', 'bookList', flatKey: 'ruleSearchList') ?? '',        ruleSearchName:            nested('ruleSearch', 'name', flatKey: 'ruleSearchName') ?? '', @@ -327,28 +357,43 @@ class BookSource {          final obj = jsonDecode(rawSourceJson);          if (obj is Map<String, dynamic>) {            final out = Map<String, dynamic>.from(obj);            out['enabled'] = enabled;            out['bookSourceGroup'] = bookSourceGroup; +          out['customOrder'] = customOrder; +          out['lastUpdateTime'] = lastUpdateTime; +          out['weight'] = weight; +          out['enabledExplore'] = enabledExplore; +          out['respondTime'] = respondTime;            return out;          }          if (obj is Map) {            final out = <String, dynamic>{              for (final e in obj.entries) e.key.toString(): e.value,            };            out['enabled'] = enabled;            out['bookSourceGroup'] = bookSourceGroup; +          out['customOrder'] = customOrder; +          out['lastUpdateTime'] = lastUpdateTime; +          out['weight'] = weight; +          out['enabledExplore'] = enabledExplore; +          out['respondTime'] = respondTime;            return out;          }        } catch (_) {}      }      return {        'bookSourceUrl': bookSourceUrl,        'bookSourceName': bookSourceName,        'bookSourceType': bookSourceType,        'bookSourceGroup': bookSourceGroup,        'enabled': enabled, +      'customOrder': customOrder, +      'lastUpdateTime': lastUpdateTime, +      'weight': weight, +      'enabledExplore': enabledExplore, +      'respondTime': respondTime,        'ruleSearchUrl': ruleSearchUrl,        'ruleSearchList': ruleSearchList,        'ruleSearchName': ruleSearchName,        'ruleSearchAuthor': ruleSearchAuthor,        'ruleSearchCoverUrl': ruleSearchCoverUrl, @@ -381,18 +426,28 @@ class BookSource {      String? bookSourceUrl,      String? bookSourceName,      String? bookSourceType,      String? bookSourceGroup,      bool? enabled, +    int? customOrder, +    int? lastUpdateTime, +    int? weight, +    bool? enabledExplore, +    int? respondTime,      String? rawSourceJson,    }) {      return BookSource(        bookSourceUrl: bookSourceUrl ?? this.bookSourceUrl,        bookSourceName: bookSourceName ?? this.bookSourceName,        bookSourceType: bookSourceType ?? this.bookSourceType,        bookSourceGroup: bookSourceGroup ?? this.bookSourceGroup,        enabled: enabled ?? this.enabled, +      customOrder: customOrder ?? this.customOrder, +      lastUpdateTime: lastUpdateTime ?? this.lastUpdateTime, +      weight: weight ?? this.weight, +      enabledExplore: enabledExplore ?? this.enabledExplore, +      respondTime: respondTime ?? this.respondTime,        ruleSearchUrl: ruleSearchUrl,        ruleSearchList: ruleSearchList,        ruleSearchName: ruleSearchName,        ruleSearchAuthor: ruleSearchAuthor,        ruleSearchCoverUrl: ruleSearchCoverUrl, diff --git a/lib/pages/explore/explore_utils.dart b/lib/pages/explore/explore_utils.dart index 00087c7..d1475a0 100644 --- a/lib/pages/explore/explore_utils.dart +++ b/lib/pages/explore/explore_utils.dart @@ -36,22 +36,18 @@ String exploreUrlOf(BookSource source) {      if (url is String) return url;    } catch (_) {}    return '';  }   +bool hasExploreUrl(BookSource source) => +    exploreUrlOf(source).trim().isNotEmpty; + +bool isExploreEnabled(BookSource source) => source.enabledExplore; +  /// 涔︽簮鏄惁鍙敤浜庡彂鐜?Tab -bool sourceHasExplore(BookSource source) { -  if (!source.enabled) return false; -  final raw = source.rawSourceJson; -  if (raw.isNotEmpty) { -    try { -      final obj = jsonDecode(raw) as Map<String, dynamic>; -      if (obj['enabledExplore'] == false) return false; -    } catch (_) {} -  } -  return exploreUrlOf(source).trim().isNotEmpty; -} +bool sourceHasExplore(BookSource source) => +    source.enabled && hasExploreUrl(source) && isExploreEnabled(source);    List<ExploreCategory> parseExploreCategories(String exploreUrlJson) {    if (exploreUrlJson.trim().isEmpty) return [];    try {      final decoded = jsonDecode(exploreUrlJson); diff --git a/lib/pages/sources/sources_page.dart b/lib/pages/sources/sources_page.dart index c841940..cf53081 100644 --- a/lib/pages/sources/sources_page.dart +++ b/lib/pages/sources/sources_page.dart @@ -1,418 +1,253 @@ +import 'dart:convert';  import 'dart:io'; -import 'package:flutter/material.dart'; +  import 'package:file_picker/file_picker.dart'; +import 'package:flutter/material.dart';  import 'package:provider/provider.dart'; +import 'package:share_plus/share_plus.dart'; +  import '../../models/book_source.dart';  import '../../providers/source_provider.dart'; -import '../../providers/book_provider.dart'; -import '../../services/local_book_service.dart'; +import '../../services/reader_font_loader.dart'; +import '../../theme/legado_tokens.dart';  import '../../widgets/error_view.dart'; -import '../../widgets/section_header.dart'; +import '../../widgets/legado_popup_menu.dart'; +import '../../widgets/source_group_manage_dialog.dart';  import '../../widgets/source_status_dot.dart';  import '../../widgets/source_validation_sheet.dart'; +import '../explore/explore_utils.dart';  import '../qrcode/qrcode_capture_page.dart'; +import '../search/search_page.dart'; +import 'source_debug_page.dart';  import 'source_editor_page.dart';  import 'source_login_page.dart';  import 'source_market_page.dart';   -enum _SourceSort { group, name, enabled } +/// 鎺掑簭 鈥?瀵归綈 Jingshiro `menu_group_sort` +enum _SourceSort { +  manual, +  auto, +  name, +  url, +  enabled, +  lastUpdate, +  respondTime, +}   -/// 涔︽簮绠＄悊椤甸潰 + 鏈湴瀵煎叆鍏ュ彛 +/// 涔︽簮绠＄悊 鈥?瀵归綈 Jingshiro [BookSourceActivity] / +/// `activity_book_source.xml` + `item_book_source.xml` + `menu/book_source.xml`  class SourcesPage extends StatefulWidget {    const SourcesPage({super.key});      @override    State<SourcesPage> createState() => _SourcesPageState();  }    class _SourcesPageState extends State<SourcesPage> { -  final TextEditingController _searchController = TextEditingController(); -  String _query = ''; -  _SourceSort _sort = _SourceSort.group; -  bool _selectionMode = false; -  final Set<String> _selectedUrls = {}; +  final _searchController = TextEditingController(); +  final _selected = <String>{}; +  final _selectBarKey = GlobalKey(); + +  _SourceSort _sort = _SourceSort.manual; +  bool _sortDesc = false; + +  /// all | enabled | disabled | login | null_group | explore_on | explore_off | group:xxx +  String _filter = 'all'; +  bool _groupByDomain = false;      @override    void dispose() {      _searchController.dispose();      super.dispose();    }   -  void _exitSelection() { -    setState(() { -      _selectionMode = false; -      _selectedUrls.clear(); -    }); -  } - -  void _enterSelection(String? firstUrl) { -    setState(() { -      _selectionMode = true; -      _selectedUrls.clear(); -      if (firstUrl != null) _selectedUrls.add(firstUrl); -    }); -  } - -  void _toggleSelected(String url) { -    setState(() { -      if (_selectedUrls.contains(url)) { -        _selectedUrls.remove(url); -        if (_selectedUrls.isEmpty) _selectionMode = false; -      } else { -        _selectedUrls.add(url); -      } -    }); +  TextStyle _uiText({ +    required Color color, +    double size = 14, +    FontWeight weight = FontWeight.w400, +  }) { +    return TextStyle( +      color: color, +      fontSize: size, +      fontWeight: weight, +      height: 1.25, +      fontFamily: ReaderFontLoader.platformSansFamily(), +      fontFamilyFallback: ReaderFontLoader.cjkFallbackFamilies(), +    );    }      List<BookSource> _visibleSources(List<BookSource> all) {      var list = List<BookSource>.from(all); -    final q = _query.trim().toLowerCase(); + +    switch (_filter) { +      case 'enabled': +        list = list.where((s) => s.enabled).toList(); +      case 'disabled': +        list = list.where((s) => !s.enabled).toList(); +      case 'login': +        list = list.where((s) => s.hasLoginConfig).toList(); +      case 'null_group': +        list = list.where((s) => s.bookSourceGroup.trim().isEmpty).toList(); +      case 'explore_on': +        list = list +            .where((s) => hasExploreUrl(s) && isExploreEnabled(s)) +            .toList(); +      case 'explore_off': +        list = list +            .where((s) => hasExploreUrl(s) && !isExploreEnabled(s)) +            .toList(); +      default: +        if (_filter.startsWith('group:')) { +          final g = _filter.substring(6); +          list = list.where((s) => s.bookSourceGroup.trim() == g).toList(); +        } +    } + +    final q = _searchController.text.trim().toLowerCase();      if (q.isNotEmpty) {        list = list            .where(              (s) =>                  s.bookSourceName.toLowerCase().contains(q) ||                  s.bookSourceUrl.toLowerCase().contains(q) ||                  s.bookSourceGroup.toLowerCase().contains(q),            )            .toList();      } -    switch (_sort) { -      case _SourceSort.name: -        list.sort( -          (a, b) => a.bookSourceName.toLowerCase().compareTo( -                b.bookSourceName.toLowerCase(), -              ), -        ); -      case _SourceSort.enabled: -        list.sort((a, b) { -          final byEnabled = (b.enabled ? 1 : 0).compareTo(a.enabled ? 1 : 0); -          if (byEnabled != 0) return byEnabled; -          return a.bookSourceName.toLowerCase().compareTo( -                b.bookSourceName.toLowerCase(), -              ); -        }); -      case _SourceSort.group: -        list.sort((a, b) { -          final ga = a.bookSourceGroup.isEmpty ? '鏈垎缁? : a.bookSourceGroup; -          final gb = b.bookSourceGroup.isEmpty ? '鏈垎缁? : b.bookSourceGroup; -          final byGroup = ga == '鏈垎缁? -              ? (gb == '鏈垎缁? ? 0 : 1) -              : (gb == '鏈垎缁? ? -1 : ga.compareTo(gb)); -          if (byGroup != 0) return byGroup; -          return a.bookSourceName.toLowerCase().compareTo( -                b.bookSourceName.toLowerCase(), -              ); -        }); + +    int cmp(BookSource a, BookSource b) { +      final r = switch (_sort) { +        _SourceSort.manual => a.customOrder.compareTo(b.customOrder), +        _SourceSort.auto => a.weight.compareTo(b.weight), +        _SourceSort.name => a.bookSourceName.toLowerCase().compareTo( +            b.bookSourceName.toLowerCase(), +          ), +        _SourceSort.url => a.bookSourceUrl.toLowerCase().compareTo( +            b.bookSourceUrl.toLowerCase(), +          ), +        _SourceSort.enabled => +          (b.enabled ? 1 : 0).compareTo(a.enabled ? 1 : 0), +        _SourceSort.lastUpdate => +          a.lastUpdateTime.compareTo(b.lastUpdateTime), +        _SourceSort.respondTime => a.respondTime.compareTo(b.respondTime), +      }; +      return _sortDesc ? -r : r;      } + +    list.sort(cmp);      return list;    }   -  @override -  Widget build(BuildContext context) { -    final theme = Theme.of(context); -    return Scaffold( -      appBar: AppBar( -        leading: _selectionMode -            ? IconButton( -                icon: const Icon(Icons.close), -                tooltip: '鍙栨秷澶氶€?, -                onPressed: _exitSelection, -              ) -            : null, -        title: Text( -          _selectionMode ? '宸查€?${_selectedUrls.length}' : '涔︽簮', -        ), -        actions: [ -          if (_selectionMode) ...[ -            IconButton( -              icon: const Icon(Icons.select_all), -              tooltip: '鍏ㄩ€夊彲瑙佷功婧?, -              onPressed: () { -                final provider = context.read<SourceProvider>(); -                final urls = -                    _visibleSources(provider.sources).map((s) => s.bookSourceUrl); -                setState(() => _selectedUrls -                  ..clear() -                  ..addAll(urls)); -              }, -            ), -          ] else ...[ -            IconButton( -              icon: const Icon(Icons.checklist_rtl), -              tooltip: '澶氶€?, -              onPressed: () => _enterSelection(null), -            ), -            PopupMenuButton<_SourceSort>( -              icon: const Icon(Icons.sort), -              tooltip: '鎺掑簭', -              initialValue: _sort, -              onSelected: (v) => setState(() => _sort = v), -              itemBuilder: (_) => const [ -                PopupMenuItem( -                  value: _SourceSort.group, -                  child: Text('鎸夊垎缁?), -                ), -                PopupMenuItem( -                  value: _SourceSort.name, -                  child: Text('鎸夊悕绉?), -                ), -                PopupMenuItem( -                  value: _SourceSort.enabled, -                  child: Text('鎸夊惎鐢ㄧ姸鎬?), -                ), -              ], -            ), -            IconButton( -              icon: const Icon(Icons.fact_check_outlined), -              tooltip: '鏍￠獙宸插惎鐢ㄤ功婧?, -              onPressed: () => _validateAllEnabled(context), -            ), -            PopupMenuButton<String>( -              icon: const Icon(Icons.more_vert), -              onSelected: (v) { -                if (v == 'import_json') { -                  _importFromJsonFile(context); -                } else if (v == 'paste_json') { -                  _showPasteDialog(context); -                } else if (v == 'import_url') { -                  _showImportUrlDialog(context); -                } else if (v == 'import_qr') { -                  _importFromQr(context); -                } -              }, -              itemBuilder: (_) => [ -                const PopupMenuItem( -                  value: 'import_json', -                  child: ListTile( -                    leading: Icon(Icons.file_open, size: 20), -                    title: Text('浠嶫SON鏂囦欢瀵煎叆'), -                    dense: true, -                    contentPadding: EdgeInsets.zero, -                  ), -                ), -                const PopupMenuItem( -                  value: 'paste_json', -                  child: ListTile( -                    leading: Icon(Icons.content_paste, size: 20), -                    title: Text('绮樿创JSON浠ｇ爜'), -                    dense: true, -                    contentPadding: EdgeInsets.zero, -                  ), -                ), -                const PopupMenuItem( -                  value: 'import_url', -                  child: ListTile( -                    leading: Icon(Icons.cloud_download, size: 20), -                    title: Text('浠嶶RL瀵煎叆'), -                    dense: true, -                    contentPadding: EdgeInsets.zero, -                  ), -                ), -                // 瀵归綈 Jingshiro book_source.xml menu_import_qr 鈫?import_by_qr_code -                const PopupMenuItem( -                  value: 'import_qr', -                  child: ListTile( -                    leading: Icon(Icons.qr_code_scanner, size: 20), -                    title: Text('浜岀淮鐮佸鍏?), -                    dense: true, -                    contentPadding: EdgeInsets.zero, -                  ), -                ), -              ], -            ), -            IconButton( -              icon: const Icon(Icons.store), -              tooltip: '涔︽簮甯傚満', -              onPressed: () => Navigator.push( -                context, -                MaterialPageRoute(builder: (_) => const SourceMarketPage()), -              ), -            ), -          ], -        ], -      ), -      body: Consumer<SourceProvider>( -        builder: (context, provider, _) { -          final sources = provider.sources; - -          if (provider.isLoading && sources.isEmpty && provider.loadError == null) { -            return const Center(child: CircularProgressIndicator()); -          } - -          if (provider.loadError != null && sources.isEmpty) { -            return ErrorView( -              message: provider.loadError!, -              onRetry: () => provider.loadSources(), -            ); -          } - -          if (sources.isEmpty) { -            return _buildEmptyState(context); -          } +  List<String> _allGroups(SourceProvider provider) => provider.knownGroups;   -          final visible = _visibleSources(sources); +  String _hostOf(BookSource s) { +    final u = Uri.tryParse(s.bookSourceUrl); +    if (u != null && u.host.isNotEmpty) return u.host; +    return '鏈煡鍩熷悕'; +  }   -          return Column( -            children: [ -              if (!_selectionMode) -                Container( -                  padding: const EdgeInsets.all(12), -                  color: theme.colorScheme.surfaceContainerLow, -                  child: Row( -                    children: [ -                      Expanded( -                        child: OutlinedButton.icon( -                          icon: const Icon(Icons.store, size: 18), -                          label: const Text('涔︽簮甯傚満'), -                          onPressed: () => Navigator.push( -                            context, -                            MaterialPageRoute( -                              builder: (_) => const SourceMarketPage(), -                            ), -                          ), -                        ), -                      ), -                      const SizedBox(width: 12), -                      Expanded( -                        child: OutlinedButton.icon( -                          icon: const Icon(Icons.file_open, size: 18), -                          label: const Text('瀵煎叆鏈湴'), -                          onPressed: () => _importLocalBook(context), -                        ), -                      ), -                    ], -                  ), -                ), -              Padding( -                padding: const EdgeInsets.fromLTRB(12, 8, 12, 4), -                child: TextField( -                  controller: _searchController, -                  decoration: InputDecoration( -                    hintText: '鎼滅储涔︽簮鍚嶇О / 鍒嗙粍 / URL', -                    prefixIcon: const Icon(Icons.search, size: 20), -                    suffixIcon: _query.isEmpty -                        ? null -                        : IconButton( -                            icon: const Icon(Icons.clear, size: 18), -                            onPressed: () { -                              _searchController.clear(); -                              setState(() => _query = ''); -                            }, -                          ), -                    isDense: true, -                    border: const OutlineInputBorder(), -                    contentPadding: const EdgeInsets.symmetric( -                      horizontal: 12, -                      vertical: 10, -                    ), -                  ), -                  onChanged: (v) => setState(() => _query = v), -                ), -              ), -              if (visible.isEmpty) -                Expanded( -                  child: Center( -                    child: Text( -                      '娌℃湁鍖归厤鐨勪功婧?, -                      style: TextStyle(color: Colors.grey[600]), -                    ), -                  ), -                ) -              else -                Expanded( -                  child: _GroupedSourceList( -                    sources: visible, -                    selectionMode: _selectionMode, -                    selectedUrls: _selectedUrls, -                    onValidate: (s) => _validateOne(context, s), -                    onToggleSelect: _toggleSelected, -                    onEnterSelection: _enterSelection, +  Widget _buildSearchField(ColorScheme scheme) { +    final onBar = scheme.onPrimary; +    return SizedBox( +      height: 32, +      child: TextField( +        controller: _searchController, +        onChanged: (_) => setState(() {}), +        style: _uiText(color: onBar), +        cursorColor: onBar, +        decoration: InputDecoration( +          hintText: '鎼滅储涔︽簮', +          hintStyle: _uiText(color: onBar.withValues(alpha: 0.72)), +          isDense: true, +          filled: true, +          fillColor: onBar.withValues(alpha: 0.14), +          contentPadding: EdgeInsets.zero, +          prefixIcon: Icon( +            Icons.search, +            size: 20, +            color: onBar.withValues(alpha: 0.85), +          ), +          prefixIconConstraints: +              const BoxConstraints(minWidth: 36, minHeight: 36), +          suffixIcon: _searchController.text.isEmpty +              ? null +              : IconButton( +                  icon: Icon( +                    Icons.close, +                    size: 18, +                    color: onBar.withValues(alpha: 0.85),                    ), +                  onPressed: () { +                    _searchController.clear(); +                    setState(() {}); +                  },                  ), -              if (_selectionMode) -                _BatchActionBar( -                  selectedCount: _selectedUrls.length, -                  onEnable: () => _batchEnable(context, true), -                  onDisable: () => _batchEnable(context, false), -                  onGroup: () => _batchSetGroup(context), -                  onValidate: () => _batchValidateSelected(context), -                  onDelete: () => _batchDelete(context), -                ), -            ], -          ); -        }, -      ), -    ); -  } - -  Widget _buildEmptyState(BuildContext context) { -    final theme = Theme.of(context); -    return Center( -      child: Column( -        mainAxisAlignment: MainAxisAlignment.center, -        children: [ -          Container( -            padding: const EdgeInsets.all(20), -            decoration: BoxDecoration( -              color: theme.colorScheme.primaryContainer.withValues(alpha: 0.3), -              shape: BoxShape.circle, -            ), -            child: Icon( -              Icons.rss_feed, -              size: 48, -              color: theme.colorScheme.primary, -            ), +          border: OutlineInputBorder( +            borderRadius: BorderRadius.circular(18), +            borderSide: BorderSide.none,            ), -          const SizedBox(height: 16), -          const Text('杩樻病鏈変功婧?, style: TextStyle(fontSize: 18)), -          const SizedBox(height: 8), -          Text( -            '鍘讳功婧愬競鍦轰竴閿鍏ワ紝鎴栦粠JSON鏂囦欢瀵煎叆', -            style: TextStyle(color: Colors.grey[600]), +          enabledBorder: OutlineInputBorder( +            borderRadius: BorderRadius.circular(18), +            borderSide: BorderSide.none,            ), -          const SizedBox(height: 24), -          FilledButton.icon( -            icon: const Icon(Icons.store), -            label: const Text('鎵撳紑涔︽簮甯傚満'), -            onPressed: () => Navigator.push( -              context, -              MaterialPageRoute(builder: (_) => const SourceMarketPage()), +          focusedBorder: OutlineInputBorder( +            borderRadius: BorderRadius.circular(18), +            borderSide: BorderSide( +              color: onBar.withValues(alpha: 0.28), +              width: 0.5,              ),            ), -          const SizedBox(height: 12), -          OutlinedButton.icon( -            icon: const Icon(Icons.file_open), -            label: const Text('浠嶫SON鏂囦欢瀵煎叆'), -            onPressed: () => _importFromJsonFile(context), -          ), -        ], +        ),        ),      );    }   -  Future<void> _batchEnable(BuildContext context, bool enabled) async { -    if (_selectedUrls.isEmpty) return; -    final provider = context.read<SourceProvider>(); -    await provider.setSourcesEnabled(_selectedUrls, enabled); -    if (!context.mounted) return; -    ScaffoldMessenger.of(context).showSnackBar( -      SnackBar( -        content: Text( -          enabled -              ? '宸插惎鐢?${_selectedUrls.length} 涓功婧? -              : '宸茬鐢?${_selectedUrls.length} 涓功婧?, -        ), -      ), -    ); -    _exitSelection(); +  void _setFilter(String filter) { +    setState(() { +      _filter = filter; +      _selected.clear(); +    }); +  } + +  Future<void> _onOverflow(String value) async { +    switch (value) { +      case 'add': +        await Navigator.push( +          context, +          MaterialPageRoute( +            builder: (_) => SourceEditorPage( +              source: BookSource( +                bookSourceUrl: '', +                bookSourceName: '', +              ), +            ), +          ), +        ); +      case 'import_local': +        await _importFromJsonFile(context); +      case 'import_online': +        _showImportUrlDialog(context); +      case 'import_qr': +        await _importFromQr(context); +      case 'group_domain': +        setState(() => _groupByDomain = !_groupByDomain); +        return; +      case 'group_manage': +        await showSourceGroupManageDialog(context); +        return; +      case 'help': +        if (!mounted) return; +        ScaffoldMessenger.of(context).showSnackBar( +          const SnackBar(content: Text('銆屽府鍔┿€嶆殏鏈疄鐜?)), +        ); +    }    }   -  Future<void> _batchDelete(BuildContext context) async { -    if (_selectedUrls.isEmpty) return; -    final count = _selectedUrls.length; +  Future<void> _deleteSelected() async { +    if (_selected.isEmpty) return; +    final count = _selected.length;      final ok = await showDialog<bool>(        context: context,        builder: (ctx) => AlertDialog(          title: const Text('鍒犻櫎涔︽簮'),          content: Text('纭畾鍒犻櫎閫変腑鐨?$count 涓功婧愶紵'), @@ -427,29 +262,36 @@ class _SourcesPageState extends State<SourcesPage> {              child: const Text('鍒犻櫎'),            ),          ],        ),      ); -    if (ok != true || !context.mounted) return; -    await context.read<SourceProvider>().deleteSources(_selectedUrls); -    if (!context.mounted) return; +    if (ok != true || !mounted) return; +    await context.read<SourceProvider>().deleteSources(_selected); +    if (!mounted) return;      ScaffoldMessenger.of(context).showSnackBar(        SnackBar(content: Text('宸插垹闄?$count 涓功婧?)),      ); -    _exitSelection(); +    setState(() => _selected.clear()); +  } + +  Future<void> _batchEnable(bool enabled) async { +    if (_selected.isEmpty) return; +    final n = _selected.length; +    await context.read<SourceProvider>().setSourcesEnabled(_selected, enabled); +    if (!mounted) return; +    ScaffoldMessenger.of(context).showSnackBar( +      SnackBar( +        content: Text(enabled ? '宸插惎鐢?$n 涓功婧? : '宸茬鐢?$n 涓功婧?), +      ), +    );    }   -  Future<void> _batchSetGroup(BuildContext context) async { -    if (_selectedUrls.isEmpty) return; +  Future<void> _batchSetGroup() async { +    if (_selected.isEmpty) return;      final provider = context.read<SourceProvider>(); -    final existing = <String>{}; -    for (final s in provider.sources) { -      if (s.bookSourceGroup.isNotEmpty) existing.add(s.bookSourceGroup); -    } -    final groups = existing.toList()..sort(); +    final groups = _allGroups(provider);      final controller = TextEditingController(); -      final group = await showDialog<String>(        context: context,        builder: (ctx) => AlertDialog(          title: const Text('璁剧疆鍒嗙粍'),          content: SizedBox( @@ -469,14 +311,16 @@ class _SourcesPageState extends State<SourcesPage> {                  const SizedBox(height: 12),                  Align(                    alignment: Alignment.centerLeft,                    child: Text(                      '宸叉湁鍒嗙粍', -                    style: TextStyle(fontSize: 12, color: Colors.grey[600]), +                    style: _uiText( +                      color: Theme.of(ctx).colorScheme.onSurfaceVariant, +                      size: 12, +                    ),                    ),                  ), -                const SizedBox(height: 8),                  ConstrainedBox(                    constraints: const BoxConstraints(maxHeight: 180),                    child: ListView(                      shrinkWrap: true,                      children: [ @@ -507,551 +351,1094 @@ class _SourcesPageState extends State<SourcesPage> {              child: const Text('纭畾'),            ),          ],        ),      ); -    if (group == null || !context.mounted) return; -    final count = _selectedUrls.length; -    await provider.setSourcesGroup(_selectedUrls, group); -    if (!context.mounted) return; +    if (group == null || !mounted) return; +    final count = _selected.length; +    await provider.setSourcesGroup(_selected, group); +    if (!mounted) return;      ScaffoldMessenger.of(context).showSnackBar(        SnackBar(          content: Text( -          group.isEmpty -              ? '宸叉竻闄?$count 涓功婧愬垎缁? -              : '宸插皢 $count 涓功婧愮Щ鑷炽€?group銆?, +          group.isEmpty ? '宸叉竻闄?$count 涓功婧愬垎缁? : '宸插皢 $count 涓功婧愮Щ鑷炽€?group銆?,          ),        ),      ); -    _exitSelection();    }   -  Future<void> _batchValidateSelected(BuildContext context) async { -    if (_selectedUrls.isEmpty) return; +  Future<void> _batchValidateSelected() async { +    if (_selected.isEmpty) return;      final provider = context.read<SourceProvider>();      final selected = provider.sources -        .where((s) => _selectedUrls.contains(s.bookSourceUrl)) +        .where((s) => _selected.contains(s.bookSourceUrl))          .toList();      if (selected.isEmpty) return; -      showDialog<void>(        context: context,        barrierDismissible: false,        builder: (ctx) => _BatchValidateDialog(          provider: provider,          sources: selected,        ),      ); -    _exitSelection();    }   -  /// 浠嶫SON鏂囦欢瀵煎叆涔︽簮 -  Future<void> _importFromJsonFile(BuildContext context) async { -    final provider = context.read<SourceProvider>(); -    try { -      final result = await FilePicker.platform.pickFiles( -        type: FileType.custom, -        allowedExtensions: ['json'], -      ); -      if (result == null || result.files.isEmpty) return; - -      final file = File(result.files.single.path!); -      final jsonText = await file.readAsString(); -      final success = await provider.importSources(jsonText); -      if (context.mounted) { -        ScaffoldMessenger.of(context).showSnackBar( -          SnackBar( -            content: Text(success ? '鉁?涔︽簮瀵煎叆鎴愬姛' : '鉂?瀵煎叆澶辫触锛岃妫€鏌ユ枃浠跺唴瀹?), -            backgroundColor: success ? null : Colors.red, -          ), -        ); -      } -    } catch (e) { -      if (context.mounted) { -        ScaffoldMessenger.of(context).showSnackBar( -          SnackBar(content: Text('鉂?瀵煎叆澶辫触: $e'), backgroundColor: Colors.red), -        ); -      } -    } +  Future<void> _batchExploreEnable(bool enabled) async { +    if (_selected.isEmpty) return; +    final n = _selected.length; +    await context +        .read<SourceProvider>() +        .setSourcesExploreEnabled(_selected, enabled); +    if (!mounted) return; +    ScaffoldMessenger.of(context).showSnackBar( +      SnackBar( +        content: Text(enabled ? '宸插惎鐢?$n 涓功婧愬彂鐜? : '宸茬鐢?$n 涓功婧愬彂鐜?), +      ), +    );    }   -  /// 绮樿创JSON浠ｇ爜 -  void _showPasteDialog(BuildContext context) { +  Future<void> _batchAddGroup() async { +    if (_selected.isEmpty) return;      final controller = TextEditingController(); -    showDialog( +    final group = await showDialog<String>(        context: context,        builder: (ctx) => AlertDialog( -        title: const Text('绮樿创涔︽簮JSON'), +        title: const Text('娣诲姞鍒嗙粍'),          content: TextField(            controller: controller, -          maxLines: 8,            decoration: const InputDecoration( -            hintText: '绮樿创 legado 涔︽簮 JSON锛屾垨涔︽簮璁㈤槄 URL...', +            hintText: '杈撳叆鍒嗙粍鍚?,              border: OutlineInputBorder(),            ), +          autofocus: true,          ),          actions: [            TextButton(              onPressed: () => Navigator.pop(ctx),              child: const Text('鍙栨秷'),            ),            FilledButton( -            onPressed: () async { -              final text = controller.text.trim(); -              Navigator.pop(ctx); -              if (text.isEmpty) return; -              final provider = context.read<SourceProvider>(); -              final success = await provider.importSources(text); -              if (context.mounted) { -                ScaffoldMessenger.of(context).showSnackBar( -                  SnackBar( -                    content: Text( -                      success -                          ? '鉁?涔︽簮瀵煎叆鎴愬姛' -                          : (provider.statusMessage.isNotEmpty -                              ? '鉂?${provider.statusMessage}' -                              : '鉂?瀵煎叆澶辫触'), -                    ), -                    backgroundColor: success ? null : Colors.red, -                  ), -                ); -              } +            onPressed: () { +              final g = controller.text.trim(); +              if (g.isEmpty) return; +              Navigator.pop(ctx, g);              }, -            child: const Text('瀵煎叆'), +            child: const Text('纭畾'),            ),          ],        ),      ); +    if (group == null || group.isEmpty || !mounted) return; +    final n = _selected.length; +    await context.read<SourceProvider>().addGroupToSources(_selected, group); +    if (!mounted) return; +    ScaffoldMessenger.of(context).showSnackBar( +      SnackBar(content: Text('宸插皢 $n 涓功婧愭坊鍔犺嚦銆?group銆?)), +    );    }   -  /// 浜岀淮鐮佸鍏?鈥?瀵归綈 Jingshiro `menu_import_qr` 鈫?QrCodeActivity 鈫?ImportBookSourceDialog -  Future<void> _importFromQr(BuildContext context) async { -    final result = await Navigator.push<String>( -      context, -      MaterialPageRoute(builder: (_) => const QrCodeCapturePage()), +  Future<void> _batchRemoveGroup() async { +    if (_selected.isEmpty) return; +    final n = _selected.length; +    await context.read<SourceProvider>().clearGroupOnSources(_selected); +    if (!mounted) return; +    ScaffoldMessenger.of(context).showSnackBar( +      SnackBar(content: Text('宸茬Щ闄?$n 涓功婧愮殑鍒嗙粍')),      ); -    if (result == null || result.isEmpty || !context.mounted) return; -    final provider = context.read<SourceProvider>(); -    final success = await provider.importSources(result); -    if (!context.mounted) return; +  } + +  Future<void> _batchMoveToTop() async { +    if (_selected.isEmpty) return; +    await context.read<SourceProvider>().moveSourcesToTop(_selected); +    if (!mounted) return;      ScaffoldMessenger.of(context).showSnackBar( -      SnackBar( -        content: Text( -          success -              ? (provider.statusMessage.isNotEmpty -                  ? '鉁?${provider.statusMessage}' -                  : '鉁?涔︽簮瀵煎叆鎴愬姛') -              : (provider.statusMessage.isNotEmpty -                  ? '鉂?${provider.statusMessage}' -                  : '鉂?瀵煎叆澶辫触'), -        ), -        backgroundColor: success ? null : Colors.red, -      ), +      SnackBar(content: Text('宸插皢 ${_selected.length} 涓功婧愮疆椤?)),      );    }   -  /// 浠?URL 瀵煎叆涔︽簮 -  void _showImportUrlDialog(BuildContext context) { -    final controller = TextEditingController(); -    showDialog( -      context: context, -      builder: (ctx) => AlertDialog( -        title: const Text('浠嶶RL瀵煎叆涔︽簮'), -        content: Column( -          mainAxisSize: MainAxisSize.min, -          children: [ -            TextField( -              controller: controller, -              decoration: const InputDecoration( -                hintText: '杈撳叆涔︽簮JSON鐨刄RL...', -                border: OutlineInputBorder(), -                prefixIcon: Icon(Icons.link), -              ), -            ), -            const SizedBox(height: 12), -            Text( -              '绀轰緥锛歕n' -              'https://www.yckceo.com/yuedu/shuyuan/json/id/7497.json\n' -              'https://www.yckceo.com/yuedu/shuyuan/json/id/7565.json', -              style: TextStyle(fontSize: 12, color: Colors.grey[500]), -            ), -          ], -        ), -        actions: [ -          TextButton( -            onPressed: () => Navigator.pop(ctx), -            child: const Text('鍙栨秷'), -          ), -          FilledButton.icon( -            icon: const Icon(Icons.cloud_download, size: 18), -            label: const Text('鑾峰彇骞跺鍏?), -            onPressed: () async { -              final url = controller.text.trim(); -              if (url.isEmpty) return; -              Navigator.pop(ctx); -              final provider = context.read<SourceProvider>(); -              final success = await provider.importSourcesFromUrl(url); -              if (context.mounted) { -                ScaffoldMessenger.of(context).showSnackBar( -                  success -                      ? const SnackBar(content: Text('鉁?涔︽簮瀵煎叆鎴愬姛')) -                      : const SnackBar( -                          content: Text('鉂?瀵煎叆澶辫触锛岃妫€鏌RL'), -                          backgroundColor: Colors.red, -                        ), -                ); -              } -            }, -          ), -        ], -      ), +  Future<void> _batchMoveToBottom() async { +    if (_selected.isEmpty) return; +    await context.read<SourceProvider>().moveSourcesToBottom(_selected); +    if (!mounted) return; +    ScaffoldMessenger.of(context).showSnackBar( +      SnackBar(content: Text('宸插皢 ${_selected.length} 涓功婧愮疆搴?)),      );    }   -  Future<void> _importLocalBook(BuildContext context) async { +  Future<void> _exportSelected() async { +    if (_selected.isEmpty) return; +    final provider = context.read<SourceProvider>(); +    final json = await provider.exportSourcesJson(_selected); +    final bytes = utf8.encode(json);      try { -      final b = await context.read<BookProvider>().importLocalBook(); -      if (b != null && context.mounted) { -        ScaffoldMessenger.of(context).showSnackBar( -          SnackBar(content: Text('宸插鍏? ${b.name}')), -        ); -      } -    } on LocalBookImportException catch (e) { -      if (context.mounted) { +      final saved = await FilePicker.platform.saveFile( +        dialogTitle: '瀵煎嚭涔︽簮', +        fileName: 'bookSource.json', +        type: FileType.custom, +        allowedExtensions: const ['json'], +        bytes: bytes, +      ); +      if (!mounted) return; +      if (saved == null) {          ScaffoldMessenger.of(context).showSnackBar( -          SnackBar(content: Text(e.message), backgroundColor: Colors.red), +          const SnackBar(content: Text('宸插彇娑堝鍑?)),          ); +        return;        } +      ScaffoldMessenger.of(context).showSnackBar( +        const SnackBar(content: Text('涔︽簮瀵煎嚭鎴愬姛')), +      ); +    } catch (_) { +      await Share.share(json, subject: '涔︽簮'); +      if (!mounted) return; +      ScaffoldMessenger.of(context).showSnackBar( +        const SnackBar(content: Text('褰撳墠骞冲彴涓嶆敮鎸佷繚瀛樻枃浠讹紝宸叉敼涓哄垎浜?)), +      ); +    } +  } + +  Future<void> _shareSelected() async { +    if (_selected.isEmpty) return; +    final json = +        await context.read<SourceProvider>().exportSourcesJson(_selected); +    try { +      await Share.share(json, subject: '涔︽簮');      } catch (e) { -      if (context.mounted) { -        ScaffoldMessenger.of(context).showSnackBar( -          SnackBar(content: Text('瀵煎叆澶辫触: $e'), backgroundColor: Colors.red), -        ); -      } +      if (!mounted) return; +      ScaffoldMessenger.of(context).showSnackBar( +        SnackBar(content: Text('鍒嗕韩澶辫触: $e'), backgroundColor: Colors.red), +      );      }    }   -  Future<void> _validateAllEnabled(BuildContext context) async { -    final provider = context.read<SourceProvider>(); -    if (!provider.sources.any((s) => s.enabled)) { +  Future<void> _onBottomMore(String value) async { +    if (_selected.isEmpty) {        ScaffoldMessenger.of(context).showSnackBar( -        const SnackBar(content: Text('娌℃湁宸插惎鐢ㄧ殑涔︽簮鍙牎楠?)), +        const SnackBar(content: Text('璇峰厛閫夋嫨涔︽簮')),        );        return;      } +    switch (value) { +      case 'enable': +        await _batchEnable(true); +      case 'disable': +        await _batchEnable(false); +      case 'explore_enable': +        await _batchExploreEnable(true); +      case 'explore_disable': +        await _batchExploreEnable(false); +      case 'group': +        await _batchSetGroup(); +      case 'add_group': +        await _batchAddGroup(); +      case 'remove_group': +        await _batchRemoveGroup(); +      case 'top': +        await _batchMoveToTop(); +      case 'bottom': +        await _batchMoveToBottom(); +      case 'export': +        await _exportSelected(); +      case 'share': +        await _shareSelected(); +      case 'validate': +        await _batchValidateSelected(); +    } +  }   -    if (!context.mounted) return; - -    showDialog<void>( +  void _showItemMenu(BookSource source, Offset anchor) { +    final provider = context.read<SourceProvider>(); +    final manual = _sort == _SourceSort.manual; +    final explore = hasExploreUrl(source); +    showMenu<String>(        context: context, -      barrierDismissible: false, -      builder: (ctx) => _BatchValidateDialog(provider: provider), -    ); +      position: RelativeRect.fromLTRB( +        anchor.dx, +        anchor.dy, +        anchor.dx, +        anchor.dy, +      ), +      items: [ +        if (manual) const PopupMenuItem(value: 'top', child: Text('缃《')), +        if (manual) const PopupMenuItem(value: 'bottom', child: Text('缃簳')), +        const PopupMenuItem(value: 'edit', child: Text('缂栬緫')), +        const PopupMenuItem(value: 'validate', child: Text('鏍￠獙')), +        if (source.hasLoginConfig) +          const PopupMenuItem(value: 'login', child: Text('鐧诲綍')), +        const PopupMenuItem(value: 'search', child: Text('鎼滅储')), +        const PopupMenuItem(value: 'debug', child: Text('璋冭瘯')), +        PopupMenuItem( +          value: 'toggle', +          child: Text(source.enabled ? '绂佺敤' : '鍚敤'), +        ), +        if (explore) +          PopupMenuItem( +            value: 'explore', +            child: Text( +              isExploreEnabled(source) ? '绂佺敤鍙戠幇' : '鍚敤鍙戠幇', +            ), +          ), +        const PopupMenuItem(value: 'del', child: Text('鍒犻櫎')), +      ], +    ).then((action) async { +      if (action == null || !mounted) return; +      switch (action) { +        case 'top': +          await provider.moveSourcesToTop([source.bookSourceUrl]); +        case 'bottom': +          await provider.moveSourcesToBottom([source.bookSourceUrl]); +        case 'edit': +          await Navigator.push( +            context, +            MaterialPageRoute( +              builder: (_) => ChangeNotifierProvider.value( +                value: provider, +                child: SourceEditorPage(source: source), +              ), +            ), +          ); +        case 'validate': +          await _validateOne(context, source); +        case 'login': +          await Navigator.push( +            context, +            MaterialPageRoute( +              builder: (_) => SourceLoginPage(source: source), +            ), +          ); +        case 'search': +          await Navigator.push( +            context, +            MaterialPageRoute( +              builder: (_) => SearchPage( +                initialRestrictSourceUrls: {source.bookSourceUrl}, +              ), +            ), +          ); +        case 'debug': +          await SourceDebugPage.open(context, source); +        case 'toggle': +          await provider.toggleSource(source.bookSourceUrl, !source.enabled); +        case 'explore': +          await provider.setSourcesExploreEnabled( +            [source.bookSourceUrl], +            !isExploreEnabled(source), +          ); +        case 'del': +          final yes = await showDialog<bool>( +            context: context, +            builder: (ctx) => AlertDialog( +              title: const Text('鍒犻櫎'), +              content: Text('纭畾鍒犻櫎\n${source.bookSourceName}锛?), +              actions: [ +                TextButton( +                  onPressed: () => Navigator.pop(ctx, false), +                  child: const Text('鍙栨秷'), +                ), +                FilledButton( +                  onPressed: () => Navigator.pop(ctx, true), +                  child: const Text('鍒犻櫎'), +                ), +              ], +            ), +          ); +          if (yes == true) { +            await provider.deleteSources([source.bookSourceUrl]); +            _selected.remove(source.bookSourceUrl); +            if (mounted) setState(() {}); +          } +      } +    });    }   -  Future<void> _validateOne(BuildContext context, BookSource source) async { -    final provider = context.read<SourceProvider>(); -    final result = await provider.validateSource(source); -    if (!context.mounted || result == null) return; -    await SourceValidationSheet.show( -      context, -      sourceName: source.bookSourceName, -      result: result, +  PopupMenuItem<String> _menuItem( +    IconData icon, +    String label, +    String value, +    ColorScheme scheme, { +    bool checked = false, +  }) { +    return PopupMenuItem( +      value: value, +      child: Row( +        children: [ +          Icon(icon, size: 20, color: scheme.onSurface), +          const SizedBox(width: 12), +          Expanded( +            child: Text(label, style: _uiText(color: scheme.onSurface)), +          ), +          if (checked) +            Icon(Icons.check, size: 18, color: scheme.secondary), +        ], +      ),      );    } -} - -class _BatchActionBar extends StatelessWidget { -  final int selectedCount; -  final VoidCallback onEnable; -  final VoidCallback onDisable; -  final VoidCallback onGroup; -  final VoidCallback onValidate; -  final VoidCallback onDelete; - -  const _BatchActionBar({ -    required this.selectedCount, -    required this.onEnable, -    required this.onDisable, -    required this.onGroup, -    required this.onValidate, -    required this.onDelete, -  });      @override    Widget build(BuildContext context) { -    final theme = Theme.of(context); -    final enabled = selectedCount > 0; -    return Material( -      elevation: 8, -      color: theme.colorScheme.surfaceContainerHigh, -      child: SafeArea( -        top: false, -        child: Padding( -          padding: const EdgeInsets.symmetric(horizontal: 4, vertical: 6), -          child: Row( -            mainAxisAlignment: MainAxisAlignment.spaceAround, -            children: [ -              _BatchAction( -                icon: Icons.check_circle_outline, -                label: '鍚敤', -                onTap: enabled ? onEnable : null, +    final scheme = Theme.of(context).colorScheme; +    final accent = scheme.secondary; + +    return Scaffold( +      appBar: AppBar( +        titleSpacing: 0, +        title: Padding( +          padding: const EdgeInsets.only(right: 4), +          child: _buildSearchField(scheme), +        ), +        actions: [ +          // 鎺掑簭 鈥?menu action_sort +          PopupMenuButton<String>( +            tooltip: '鎺掑簭', +            icon: const Icon(Icons.sort_by_alpha), +            offset: legadoAppBarPopupOffset(context), +            onSelected: (v) { +              setState(() { +                if (v == 'desc') { +                  _sortDesc = !_sortDesc; +                } else { +                  _sort = switch (v) { +                    'manual' => _SourceSort.manual, +                    'auto' => _SourceSort.auto, +                    'name' => _SourceSort.name, +                    'url' => _SourceSort.url, +                    'enabled' => _SourceSort.enabled, +                    'lastUpdate' => _SourceSort.lastUpdate, +                    'respondTime' => _SourceSort.respondTime, +                    _ => _sort, +                  }; +                } +              }); +            }, +            itemBuilder: (_) => [ +              CheckedPopupMenuItem( +                value: 'desc', +                checked: _sortDesc, +                child: Text('闄嶅簭', style: _uiText(color: scheme.onSurface)), +              ), +              const PopupMenuDivider(), +              CheckedPopupMenuItem( +                value: 'manual', +                checked: _sort == _SourceSort.manual, +                child: Text('鎵嬪姩', style: _uiText(color: scheme.onSurface)), +              ), +              CheckedPopupMenuItem( +                value: 'auto', +                checked: _sort == _SourceSort.auto, +                child: Text('鏅鸿兘鎺掑簭', style: _uiText(color: scheme.onSurface)), +              ), +              CheckedPopupMenuItem( +                value: 'name', +                checked: _sort == _SourceSort.name, +                child: Text('鍚嶇О', style: _uiText(color: scheme.onSurface)),                ), -              _BatchAction( -                icon: Icons.block, -                label: '绂佺敤', -                onTap: enabled ? onDisable : null, +              CheckedPopupMenuItem( +                value: 'url', +                checked: _sort == _SourceSort.url, +                child: Text('URL', style: _uiText(color: scheme.onSurface)),                ), -              _BatchAction( -                icon: Icons.folder_outlined, -                label: '鍒嗙粍', -                onTap: enabled ? onGroup : null, +              CheckedPopupMenuItem( +                value: 'enabled', +                checked: _sort == _SourceSort.enabled, +                child: Text('鍚敤鐘舵€?, style: _uiText(color: scheme.onSurface)),                ), -              _BatchAction( -                icon: Icons.fact_check_outlined, -                label: '鏍￠獙', -                onTap: enabled ? onValidate : null, +              CheckedPopupMenuItem( +                value: 'lastUpdate', +                checked: _sort == _SourceSort.lastUpdate, +                child: Text('鏇存柊鏃堕棿鎺掑簭', style: _uiText(color: scheme.onSurface)),                ), -              _BatchAction( -                icon: Icons.delete_outline, -                label: '鍒犻櫎', -                color: Colors.red, -                onTap: enabled ? onDelete : null, +              CheckedPopupMenuItem( +                value: 'respondTime', +                checked: _sort == _SourceSort.respondTime, +                child: Text('鍝嶅簲鏃堕棿鎺掑簭', style: _uiText(color: scheme.onSurface)),                ),              ],            ), -        ), +          // 鍒嗙粍绛涢€?鈥?menu_group +          Consumer<SourceProvider>( +            builder: (context, provider, _) { +              final groups = _allGroups(provider); +              return PopupMenuButton<String>( +                tooltip: '鍒嗙粍', +                icon: const Icon(Icons.hub_outlined), +                offset: legadoAppBarPopupOffset(context), +                onSelected: (v) { +                  if (v == 'manage') { +                    showSourceGroupManageDialog(context); +                    return; +                  } +                  _setFilter(v); +                }, +                itemBuilder: (_) => [ +                  PopupMenuItem( +                    value: 'manage', +                    child: +                        Text('鍒嗙粍绠＄悊', style: _uiText(color: scheme.onSurface)), +                  ), +                  PopupMenuItem( +                    value: 'all', +                    child: Text('鍏ㄩ儴', style: _uiText(color: scheme.onSurface)), +                  ), +                  PopupMenuItem( +                    value: 'enabled', +                    child: +                        Text('宸插惎鐢?, style: _uiText(color: scheme.onSurface)), +                  ), +                  PopupMenuItem( +                    value: 'disabled', +                    child: +                        Text('宸茬鐢?, style: _uiText(color: scheme.onSurface)), +                  ), +                  PopupMenuItem( +                    value: 'login', +                    child: +                        Text('闇€瑕佺櫥褰?, style: _uiText(color: scheme.onSurface)), +                  ), +                  PopupMenuItem( +                    value: 'null_group', +                    child: +                        Text('鏈垎缁?, style: _uiText(color: scheme.onSurface)), +                  ), +                  PopupMenuItem( +                    value: 'explore_on', +                    child: +                        Text('鍚敤鍙戠幇', style: _uiText(color: scheme.onSurface)), +                  ), +                  PopupMenuItem( +                    value: 'explore_off', +                    child: +                        Text('绂佺敤鍙戠幇', style: _uiText(color: scheme.onSurface)), +                  ), +                  if (groups.isNotEmpty) const PopupMenuDivider(), +                  ...groups.map( +                    (g) => PopupMenuItem( +                      value: 'group:$g', +                      child: Text(g, style: _uiText(color: scheme.onSurface)), +                    ), +                  ), +                ], +              ); +            }, +          ), +          // 鏇村 鈥?瀵归綈 Jingshiro menu/book_source.xml +          PopupMenuButton<String>( +            tooltip: '鏇村', +            icon: const Icon(Icons.more_vert), +            offset: legadoAppBarPopupOffset(context), +            onSelected: _onOverflow, +            itemBuilder: (_) => [ +              _menuItem(Icons.add, '鏂板缓涔︽簮', 'add', scheme), +              _menuItem( +                Icons.file_download_outlined, +                '鏈湴瀵煎叆', +                'import_local', +                scheme, +              ), +              _menuItem( +                Icons.cloud_download_outlined, +                '缃戠粶瀵煎叆', +                'import_online', +                scheme, +              ), +              _menuItem(Icons.qr_code_scanner, '浜岀淮鐮佸鍏?, 'import_qr', scheme), +              _menuItem( +                Icons.dns_outlined, +                '鎸夊煙鍚嶅垎缁?, +                'group_domain', +                scheme, +                checked: _groupByDomain, +              ), +              _menuItem(Icons.help_outline, '甯姪', 'help', scheme), +            ], +          ), +        ],        ), -    ); -  } -} +      body: Consumer<SourceProvider>( +        builder: (context, provider, _) { +          final sources = provider.sources;   -class _BatchAction extends StatelessWidget { -  final IconData icon; -  final String label; -  final VoidCallback? onTap; -  final Color? color; +          if (provider.isLoading && +              sources.isEmpty && +              provider.loadError == null) { +            return const Center(child: CircularProgressIndicator()); +          } +          if (provider.loadError != null && sources.isEmpty) { +            return ErrorView( +              message: provider.loadError!, +              onRetry: () => provider.loadSources(), +            ); +          } +          if (sources.isEmpty) { +            return _buildEmptyState(scheme); +          }   -  const _BatchAction({ -    required this.icon, -    required this.label, -    this.onTap, -    this.color, -  }); +          final visible = _visibleSources(sources); +          if (visible.isEmpty) { +            return Center( +              child: Text( +                '娌℃湁鍖归厤鐨勪功婧?, +                style: _uiText(color: scheme.onSurfaceVariant), +              ), +            ); +          }   -  @override -  Widget build(BuildContext context) { -    final theme = Theme.of(context); -    final c = onTap == null -        ? theme.disabledColor -        : (color ?? theme.colorScheme.onSurface); -    return InkWell( -      onTap: onTap, -      borderRadius: BorderRadius.circular(8), -      child: Padding( -        padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 6), -        child: Column( -          mainAxisSize: MainAxisSize.min, -          children: [ -            Icon(icon, size: 22, color: c), -            const SizedBox(height: 2), -            Text(label, style: TextStyle(fontSize: 11, color: c)), -          ], -        ), +          return Column( +            children: [ +              Expanded( +                child: _groupByDomain +                    ? _buildDomainGroupedList( +                        visible, +                        provider, +                        scheme, +                        accent, +                      ) +                    : _buildFlatList(visible, provider, scheme, accent), +              ), +              _buildSelectActionBar(scheme, visible), +            ], +          ); +        },        ),      );    } -}   -/// 鎸?bookSourceGroup 鍒嗙粍鐨勪功婧愬垪琛?-class _GroupedSourceList extends StatelessWidget { -  final List<BookSource> sources; -  final bool selectionMode; -  final Set<String> selectedUrls; -  final void Function(BookSource source) onValidate; -  final void Function(String url) onToggleSelect; -  final void Function(String url) onEnterSelection; +  Widget _buildEmptyState(ColorScheme scheme) { +    return Center( +      child: Column( +        mainAxisAlignment: MainAxisAlignment.center, +        children: [ +          Icon( +            Icons.library_books_outlined, +            size: 48, +            color: scheme.primary, +          ), +          const SizedBox(height: 16), +          Text('杩樻病鏈変功婧?, style: _uiText(color: scheme.onSurface, size: 18)), +          const SizedBox(height: 8), +          Text( +            '鍙充笂瑙掕彍鍗曞彲鏂板缓鎴栧鍏ヤ功婧?, +            style: _uiText(color: scheme.onSurfaceVariant, size: 13), +          ), +          const SizedBox(height: 24), +          FilledButton.icon( +            icon: const Icon(Icons.store), +            label: const Text('鎵撳紑涔︽簮甯傚満'), +            onPressed: () => Navigator.push( +              context, +              MaterialPageRoute(builder: (_) => const SourceMarketPage()), +            ), +          ), +        ], +      ), +    ); +  }   -  const _GroupedSourceList({ -    required this.sources, -    required this.selectionMode, -    required this.selectedUrls, -    required this.onValidate, -    required this.onToggleSelect, -    required this.onEnterSelection, -  }); +  Widget _buildFlatList( +    List<BookSource> visible, +    SourceProvider provider, +    ColorScheme scheme, +    Color accent, +  ) { +    final canReorder = _sort == _SourceSort.manual && !_groupByDomain;   -  Map<String, List<BookSource>> _grouped() { -    final map = <String, List<BookSource>>{}; -    for (final s in sources) { -      final g = s.bookSourceGroup.isNotEmpty ? s.bookSourceGroup : '鏈垎缁?; -      map.putIfAbsent(g, () => []).add(s); +    if (canReorder) { +      return ReorderableListView.builder( +        buildDefaultDragHandles: false, +        itemCount: visible.length, +        onReorder: (oldIndex, newIndex) async { +          var target = newIndex; +          if (oldIndex < target) target--; +          final items = List<BookSource>.from(visible); +          final moved = items.removeAt(oldIndex); +          items.insert(target, moved); +          final urls = items.map((s) => s.bookSourceUrl).toList(); +          await provider.reorderSources(urls); +        }, +        itemBuilder: (_, i) { +          final s = visible[i]; +          return Column( +            key: ValueKey(s.bookSourceUrl), +            mainAxisSize: MainAxisSize.min, +            children: [ +              _buildSourceRow( +                s, +                provider, +                scheme, +                accent, +                dragHandle: ReorderableDragStartListener( +                  index: i, +                  child: Icon( +                    Icons.drag_handle, +                    size: 22, +                    color: scheme.onSurfaceVariant, +                  ), +                ), +              ), +              Divider( +                height: 1, +                color: scheme.outlineVariant.withValues(alpha: 0.35), +              ), +            ], +          ); +        }, +      );      } -    return map; -  }   -  @override -  Widget build(BuildContext context) { -    final groups = _grouped(); -    final keys = groups.keys.toList() -      ..sort((a, b) => a == '鏈垎缁? ? 1 : b == '鏈垎缁? ? -1 : a.compareTo(b)); +    return ListView.separated( +      itemCount: visible.length, +      separatorBuilder: (_, _) => Divider( +        height: 1, +        color: scheme.outlineVariant.withValues(alpha: 0.35), +      ), +      itemBuilder: (_, i) => _buildSourceRow( +        visible[i], +        provider, +        scheme, +        accent, +      ), +    ); +  }   +  Widget _buildDomainGroupedList( +    List<BookSource> visible, +    SourceProvider provider, +    ColorScheme scheme, +    Color accent, +  ) { +    final map = <String, List<BookSource>>{}; +    for (final s in visible) { +      map.putIfAbsent(_hostOf(s), () => []).add(s); +    } +    final keys = map.keys.toList()..sort();      return ListView.builder( -      padding: const EdgeInsets.symmetric(vertical: 4),        itemCount: keys.length,        itemBuilder: (_, gi) { -        final groupName = keys[gi]; -        final items = groups[groupName]!; +        final host = keys[gi]; +        final items = map[host]!;          return Column(            crossAxisAlignment: CrossAxisAlignment.start,            children: [ -            SectionHeader('$groupName (${items.length})'), +            Padding( +              padding: const EdgeInsets.fromLTRB(16, 12, 16, 4), +              child: Text( +                host, +                style: _uiText( +                  color: scheme.secondary, +                  size: 16, +                  weight: FontWeight.w500, +                ), +              ), +            ),              ...items.map(                (s) => Column(                  children: [ -                  _SourceTile( -                    source: s, -                    selectionMode: selectionMode, -                    selected: selectedUrls.contains(s.bookSourceUrl), -                    onValidate: () => onValidate(s), -                    onToggleSelect: () => onToggleSelect(s.bookSourceUrl), -                    onEnterSelection: () => onEnterSelection(s.bookSourceUrl), +                  _buildSourceRow(s, provider, scheme, accent), +                  Divider( +                    height: 1, +                    color: scheme.outlineVariant.withValues(alpha: 0.35),                    ), -                  const Divider(height: 1, indent: 72),                  ],                ),              ),            ],          );        },      );    } -} - -/// 鍗曚釜涔︽簮 Tile -class _SourceTile extends StatelessWidget { -  final BookSource source; -  final bool selectionMode; -  final bool selected; -  final VoidCallback onValidate; -  final VoidCallback onToggleSelect; -  final VoidCallback onEnterSelection;   -  const _SourceTile({ -    required this.source, -    required this.selectionMode, -    required this.selected, -    required this.onValidate, -    required this.onToggleSelect, -    required this.onEnterSelection, -  }); +  /// 瀵归綈 `item_book_source.xml`锛氬嬀閫?鍚?| Switch | 缂栬緫 | 鏇村 | 鍙戠幇缁跨偣 +  Widget _buildSourceRow( +    BookSource s, +    SourceProvider provider, +    ColorScheme scheme, +    Color accent, { +    Widget? dragHandle, +  }) { +    final checked = _selected.contains(s.bookSourceUrl); +    final validating = provider.isValidating && +        provider.validatingSourceUrl == s.bookSourceUrl; +    final validation = provider.validationOf(s.bookSourceUrl); +    final hasExplore = hasExploreUrl(s); +    final group = s.bookSourceGroup.trim(); +    final displayName = +        group.isEmpty ? s.bookSourceName : '${s.bookSourceName} ($group)';   -  @override -  Widget build(BuildContext context) { -    final theme = Theme.of(context); -    return Consumer<SourceProvider>( -      builder: (context, provider, _) { -        final validating = -            provider.isValidating && -            provider.validatingSourceUrl == source.bookSourceUrl; -        final validation = provider.validationOf(source.bookSourceUrl); - -        return ListTile( -          selected: selectionMode && selected, -          leading: selectionMode -              ? Checkbox( -                  value: selected, -                  onChanged: (_) => onToggleSelect(), -                ) -              : CircleAvatar( -                  backgroundColor: source.enabled -                      ? theme.colorScheme.primaryContainer -                      : Colors.grey[200], -                  radius: 18, -                  child: validating -                      ? SizedBox( -                          width: 18, -                          height: 18, -                          child: CircularProgressIndicator( -                            strokeWidth: 2, -                            color: theme.colorScheme.primary, -                          ), -                        ) -                      : Icon( -                          Icons.rss_feed, -                          size: 20, -                          color: source.enabled -                              ? theme.colorScheme.primary -                              : Colors.grey, +    return Material( +      color: scheme.surface, +      child: Padding( +        padding: const EdgeInsets.symmetric( +          horizontal: LegadoTokens.spacingMd, +          vertical: 10, +        ), +        child: Row( +          children: [ +            if (dragHandle != null) ...[ +              dragHandle, +              const SizedBox(width: 4), +            ], +            Expanded( +              child: CheckboxListTile( +                value: checked, +                onChanged: (v) { +                  setState(() { +                    if (v == true) { +                      _selected.add(s.bookSourceUrl); +                    } else { +                      _selected.remove(s.bookSourceUrl); +                    } +                  }); +                }, +                controlAffinity: ListTileControlAffinity.leading, +                contentPadding: EdgeInsets.zero, +                dense: true, +                visualDensity: VisualDensity.compact, +                title: Row( +                  children: [ +                    SourceStatusDot(source: s, validation: validation), +                    Expanded( +                      child: Text( +                        displayName, +                        maxLines: 1, +                        overflow: TextOverflow.ellipsis, +                        style: _uiText( +                          color: s.enabled +                              ? scheme.onSurface +                              : scheme.onSurface.withValues(alpha: 0.55), +                          size: 15,                          ), +                      ), +                    ), +                    if (validating) +                      const SizedBox( +                        width: 16, +                        height: 16, +                        child: CircularProgressIndicator(strokeWidth: 2), +                      ), +                  ],                  ), -          title: Row( +              ), +            ), +            Switch( +              value: s.enabled, +              activeThumbColor: Colors.white, +              activeTrackColor: accent, +              onChanged: (v) => +                  provider.toggleSource(s.bookSourceUrl, v), +            ), +            IconButton( +              tooltip: '缂栬緫', +              icon: Icon(Icons.open_in_new, size: 20, color: scheme.onSurface), +              onPressed: () => Navigator.push( +                context, +                MaterialPageRoute( +                  builder: (_) => ChangeNotifierProvider.value( +                    value: provider, +                    child: SourceEditorPage(source: s), +                  ), +                ), +              ), +            ), +            Stack( +              clipBehavior: Clip.none, +              children: [ +                Builder( +                  builder: (tileCtx) => IconButton( +                    tooltip: '鏇村', +                    icon: Icon( +                      Icons.more_vert, +                      size: 20, +                      color: scheme.onSurface, +                    ), +                    onPressed: () { +                      final box = tileCtx.findRenderObject() as RenderBox?; +                      final offset = +                          box?.localToGlobal(Offset.zero) ?? Offset.zero; +                      _showItemMenu(s, offset); +                    }, +                  ), +                ), +                if (hasExplore) +                  Positioned( +                    right: 8, +                    top: 8, +                    child: Container( +                      width: 8, +                      height: 8, +                      decoration: BoxDecoration( +                        color: isExploreEnabled(s) +                            ? const Color(0xFF43A047) +                            : const Color(0xFFE53935), +                        shape: BoxShape.circle, +                      ), +                    ), +                  ), +              ], +            ), +          ], +        ), +      ), +    ); +  } + +  /// 瀵归綈 `view_select_action_bar.xml` +  ButtonStyle _selectBarButtonStyle(ColorScheme scheme) { +    return OutlinedButton.styleFrom( +      padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 4), +      minimumSize: const Size(0, 32), +      tapTargetSize: MaterialTapTargetSize.shrinkWrap, +      visualDensity: VisualDensity.compact, +      foregroundColor: scheme.onSurface, +      side: BorderSide(color: scheme.outline.withValues(alpha: 0.55)), +      shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(4)), +    ); +  } + +  Widget _buildSelectActionBar(ColorScheme scheme, List<BookSource> visible) { +    final total = visible.length; +    final selectedCount = +        visible.where((s) => _selected.contains(s.bookSourceUrl)).length; +    final allSelected = total > 0 && selectedCount == total; +    final hasSelection = selectedCount > 0; + +    final bottomBg = +        Theme.of(context).bottomAppBarTheme.color ?? scheme.surface; +    return Material( +      key: _selectBarKey, +      elevation: 2, +      color: bottomBg, +      child: Padding( +        padding: const EdgeInsets.symmetric(horizontal: 4, vertical: 6), +        child: Row(              children: [ -              SourceStatusDot(source: source, validation: validation), +              Checkbox( +                value: allSelected +                    ? true +                    : (selectedCount > 0 ? null : false), +                tristate: true, +                materialTapTargetSize: MaterialTapTargetSize.shrinkWrap, +                visualDensity: VisualDensity.compact, +                onChanged: (_) { +                  setState(() { +                    if (allSelected) { +                      for (final s in visible) { +                        _selected.remove(s.bookSourceUrl); +                      } +                    } else { +                      for (final s in visible) { +                        _selected.add(s.bookSourceUrl); +                      } +                    } +                  }); +                }, +              ),                Expanded(                  child: Text( -                  source.bookSourceName, -                  style: TextStyle( -                    fontWeight: FontWeight.w500, -                    color: source.enabled ? null : Colors.grey, +                  '鍏ㄩ€?($selectedCount/$total)', +                  maxLines: 1, +                  overflow: TextOverflow.ellipsis, +                  style: _uiText(color: scheme.onSurface, size: 14), +                ), +              ), +              OutlinedButton( +                style: _selectBarButtonStyle(scheme), +                onPressed: total == 0 +                    ? null +                    : () { +                        setState(() { +                          for (final s in visible) { +                            if (_selected.contains(s.bookSourceUrl)) { +                              _selected.remove(s.bookSourceUrl); +                            } else { +                              _selected.add(s.bookSourceUrl); +                            } +                          } +                        }); +                      }, +                child: Text('鍙嶉€?, style: _uiText(color: scheme.onSurface)), +              ), +              const SizedBox(width: 6), +              OutlinedButton( +                style: _selectBarButtonStyle(scheme), +                onPressed: hasSelection ? _deleteSelected : null, +                child: Text( +                  '鍒犻櫎', +                  style: _uiText( +                    color: hasSelection +                        ? scheme.onSurface +                        : scheme.onSurface.withValues(alpha: 0.38),                    ),                  ),                ), -              SourceValidationBadge(result: validation), +              LegadoBottomBarPopupButton<String>( +                barKey: _selectBarKey, +                onSelected: (v) => _onBottomMore(v), +                icon: Icon(Icons.more_vert, color: scheme.onSurface, size: 22), +                itemBuilder: (_) => [ +                  PopupMenuItem( +                    value: 'enable', +                    child: Text('鍚敤鎵€閫?, style: _uiText(color: scheme.onSurface)), +                  ), +                  PopupMenuItem( +                    value: 'disable', +                    child: Text('绂佺敤鎵€閫?, style: _uiText(color: scheme.onSurface)), +                  ), +                  PopupMenuItem( +                    value: 'explore_enable', +                    child: Text('鍚敤鍙戠幇', style: _uiText(color: scheme.onSurface)), +                  ), +                  PopupMenuItem( +                    value: 'explore_disable', +                    child: Text('绂佺敤鍙戠幇', style: _uiText(color: scheme.onSurface)), +                  ), +                  PopupMenuItem( +                    value: 'group', +                    child: Text('璁剧疆鍒嗙粍', style: _uiText(color: scheme.onSurface)), +                  ), +                  PopupMenuItem( +                    value: 'add_group', +                    child: Text('娣诲姞鍒嗙粍', style: _uiText(color: scheme.onSurface)), +                  ), +                  PopupMenuItem( +                    value: 'remove_group', +                    child: Text('绉婚櫎鍒嗙粍', style: _uiText(color: scheme.onSurface)), +                  ), +                  PopupMenuItem( +                    value: 'top', +                    child: Text('缃《鎵€閫?, style: _uiText(color: scheme.onSurface)), +                  ), +                  PopupMenuItem( +                    value: 'bottom', +                    child: Text('缃簳鎵€閫?, style: _uiText(color: scheme.onSurface)), +                  ), +                  PopupMenuItem( +                    value: 'export', +                    child: Text('瀵煎嚭鎵€閫?, style: _uiText(color: scheme.onSurface)), +                  ), +                  PopupMenuItem( +                    value: 'share', +                    child: Text('鍒嗕韩涔︽簮', style: _uiText(color: scheme.onSurface)), +                  ), +                  PopupMenuItem( +                    value: 'validate', +                    child: Text('鏍￠獙鎵€閫?, style: _uiText(color: scheme.onSurface)), +                  ), +                ], +              ),              ],            ), -          subtitle: Text( -            source.bookSourceGroup.isNotEmpty -                ? '${source.bookSourceGroup} | ${source.bookSourceUrl}' -                : source.bookSourceUrl, -            maxLines: 1, -            overflow: TextOverflow.ellipsis, -            style: const TextStyle(fontSize: 11), +        ), +    ); +  } + +  // 鈹€鈹€ 瀵煎叆 / 鏍￠獙锛堜繚鐣欏師涓氬姟锛?鈹€鈹€ + +  Future<void> _importFromJsonFile(BuildContext context) async { +    final provider = context.read<SourceProvider>(); +    try { +      final result = await FilePicker.platform.pickFiles( +        type: FileType.custom, +        allowedExtensions: ['json'], +      ); +      if (result == null || result.files.isEmpty) return; +      final file = File(result.files.single.path!); +      final jsonText = await file.readAsString(); +      final success = await provider.importSources(jsonText); +      if (context.mounted) { +        ScaffoldMessenger.of(context).showSnackBar( +          SnackBar( +            content: Text(success ? '涔︽簮瀵煎叆鎴愬姛' : '瀵煎叆澶辫触锛岃妫€鏌ユ枃浠跺唴瀹?), +            backgroundColor: success ? null : Colors.red,            ), -          trailing: selectionMode -              ? null -              : Row( -                  mainAxisSize: MainAxisSize.min, -                  children: [ -                    if (source.hasLoginConfig) -                      IconButton( -                        tooltip: '鐧诲綍', -                        icon: const Icon(Icons.login, size: 20), -                        onPressed: () { -                          Navigator.push( -                            context, -                            MaterialPageRoute( -                              builder: (_) => SourceLoginPage(source: source), -                            ), -                          ); -                        }, -                      ), -                    Switch( -                      value: source.enabled, -                      onChanged: (v) => -                          provider.toggleSource(source.bookSourceUrl, v), -                    ), -                  ], -                ), -          onLongPress: selectionMode ? null : onEnterSelection, -          onTap: () { -            if (selectionMode) { -              onToggleSelect(); -              return; -            } -            Navigator.push( -              context, -              MaterialPageRoute( -                builder: (_) => ChangeNotifierProvider.value( -                  value: provider, -                  child: SourceEditorPage(source: source), -                ), -              ), -            ); -          },          ); -      }, +      } +    } catch (e) { +      if (context.mounted) { +        ScaffoldMessenger.of(context).showSnackBar( +          SnackBar(content: Text('瀵煎叆澶辫触: $e'), backgroundColor: Colors.red), +        ); +      } +    } +  } + +  Future<void> _importFromQr(BuildContext context) async { +    final result = await Navigator.push<String>( +      context, +      MaterialPageRoute(builder: (_) => const QrCodeCapturePage()), +    ); +    if (result == null || result.isEmpty || !context.mounted) return; +    final provider = context.read<SourceProvider>(); +    final success = await provider.importSources(result); +    if (!context.mounted) return; +    ScaffoldMessenger.of(context).showSnackBar( +      SnackBar( +        content: Text( +          success +              ? (provider.statusMessage.isNotEmpty +                  ? provider.statusMessage +                  : '涔︽簮瀵煎叆鎴愬姛') +              : (provider.statusMessage.isNotEmpty +                  ? provider.statusMessage +                  : '瀵煎叆澶辫触'), +        ), +        backgroundColor: success ? null : Colors.red, +      ), +    ); +  } + +  void _showImportUrlDialog(BuildContext context) { +    final controller = TextEditingController(); +    showDialog( +      context: context, +      builder: (ctx) => AlertDialog( +        title: const Text('缃戠粶瀵煎叆'), +        content: TextField( +          controller: controller, +          decoration: const InputDecoration( +            hintText: '杈撳叆涔︽簮 JSON 鐨?URL鈥?, +            border: OutlineInputBorder(), +            prefixIcon: Icon(Icons.link), +          ), +        ), +        actions: [ +          TextButton( +            onPressed: () => Navigator.pop(ctx), +            child: const Text('鍙栨秷'), +          ), +          FilledButton( +            onPressed: () async { +              final url = controller.text.trim(); +              if (url.isEmpty) return; +              Navigator.pop(ctx); +              final provider = context.read<SourceProvider>(); +              final success = await provider.importSourcesFromUrl(url); +              if (context.mounted) { +                ScaffoldMessenger.of(context).showSnackBar( +                  success +                      ? const SnackBar(content: Text('涔︽簮瀵煎叆鎴愬姛')) +                      : const SnackBar( +                          content: Text('瀵煎叆澶辫触锛岃妫€鏌?URL'), +                          backgroundColor: Colors.red, +                        ), +                ); +              } +            }, +            child: const Text('瀵煎叆'), +          ), +        ], +      ), +    ); +  } + +  Future<void> _validateOne(BuildContext context, BookSource source) async { +    final provider = context.read<SourceProvider>(); +    final result = await provider.validateSource(source); +    if (!context.mounted || result == null) return; +    await SourceValidationSheet.show( +      context, +      sourceName: source.bookSourceName, +      result: result,      );    }  }    class _BatchValidateDialog extends StatefulWidget { diff --git a/lib/providers/source_order.dart b/lib/providers/source_order.dart new file mode 100644 index 0000000..c7f46c3 --- /dev/null +++ b/lib/providers/source_order.dart @@ -0,0 +1,35 @@ +import '../models/book_source.dart'; + +List<BookSource> sourcesInManualOrder(List<BookSource> all) { +  final ordered = List<BookSource>.from(all); +  ordered.sort((a, b) { +    final byOrder = a.customOrder.compareTo(b.customOrder); +    if (byOrder != 0) return byOrder; +    return a.bookSourceUrl.compareTo(b.bookSourceUrl); +  }); +  return ordered; +} + +Map<String, int> customOrdersAfterMoveToTop( +  List<BookSource> all, +  Set<String> selected, +) { +  final sel = all.where((s) => selected.contains(s.bookSourceUrl)).toList(); +  final rest = all.where((s) => !selected.contains(s.bookSourceUrl)).toList(); +  final ordered = [...sel, ...rest]; +  return { +    for (var i = 0; i < ordered.length; i++) ordered[i].bookSourceUrl: i, +  }; +} + +Map<String, int> customOrdersAfterMoveToBottom( +  List<BookSource> all, +  Set<String> selected, +) { +  final rest = all.where((s) => !selected.contains(s.bookSourceUrl)).toList(); +  final sel = all.where((s) => selected.contains(s.bookSourceUrl)).toList(); +  final ordered = [...rest, ...sel]; +  return { +    for (var i = 0; i < ordered.length; i++) ordered[i].bookSourceUrl: i, +  }; +} diff --git a/lib/providers/source_provider.dart b/lib/providers/source_provider.dart index 924b5e8..47c9835 100644 --- a/lib/providers/source_provider.dart +++ b/lib/providers/source_provider.dart @@ -6,10 +6,12 @@ import '../models/book.dart';  import '../models/book_source.dart';  import '../models/source_validation_result.dart';  import '../bridge/legado_engine_bridge.dart';  import '../database/dao/source_dao.dart';  import '../services/book_source_service.dart'; +import '../services/source_group_catalog.dart'; +import 'source_order.dart';    /// 涔︽簮绠＄悊 Provider 鈥?涔︽簮 CRUD銆佹悳绱? class SourceProvider extends ChangeNotifier {    final SourceDao _dao = SourceDao();    final BookSourceService _sourceService = BookSourceService(); @@ -34,17 +36,31 @@ class SourceProvider extends ChangeNotifier {    String? get loadError => _loadError;      SourceValidationResult? validationOf(String sourceUrl) =>        _validationResults[sourceUrl];   +  /// 鍒嗙粍绛涢€?绠＄悊鐢細鐩綍 鈭?涔︽簮宸叉湁鍒嗙粍锛堜笉寮哄埗鏀逛功婧愶級 +  List<String> get knownGroups { +    final set = SourceGroupCatalog.names.toSet(); +    for (final s in _sources) { +      final g = s.bookSourceGroup.trim(); +      if (g.isNotEmpty) set.add(g); +    } +    return set.toList()..sort(); +  } +    /// 鍔犺浇涔︽簮    Future<void> loadSources() async {      _isLoading = true;      _loadError = null;      notifyListeners();      try { +      await SourceGroupCatalog.load();        _sources = await _dao.getAll(); +      await SourceGroupCatalog.mergeFromSources( +        _sources.map((s) => s.bookSourceGroup), +      );        _loadError = null;      } catch (e) {        _loadError = '鍔犺浇涔︽簮澶辫触: $e';        debugPrint(_loadError);      } finally { @@ -58,10 +74,44 @@ class SourceProvider extends ChangeNotifier {      await _dao.upsert(source);      _sources = await _dao.getAll();      notifyListeners();    }   +  /// 閲嶅懡鍚嶅垎缁勶紙鐩綍 + 鎵€鏈夎鍒嗙粍涔︽簮锛?+  Future<void> renameGroup(String oldName, String newName) async { +    if (oldName == newName) return; +    await SourceGroupCatalog.rename(oldName, newName); +    for (final s in _sources) { +      if (s.bookSourceGroup.trim() == oldName) { +        await _dao.update(s.copyWith(bookSourceGroup: newName)); +      } +    } +    _sources = await _dao.getAll(); +    notifyListeners(); +  } + +  /// 娣诲姞鍒嗙粍鍚嶅埌鐩綍锛?*涓?*鏀瑰姩浠讳綍涔︽簮锛堥伩鍏嶆湭鍒嗙粍婧愯鎵归噺鏀瑰啓锛夈€?+  Future<bool> addGroup(String group) async { +    final name = group.trim(); +    if (name.isEmpty) return false; +    final added = await SourceGroupCatalog.add(name); +    if (added) notifyListeners(); +    return added; +  } + +  /// 鍒犻櫎鍒嗙粍锛堢洰褰曠Щ闄わ紱璇ヤ功婧?group 娓呯┖锛?+  Future<void> deleteGroup(String groupName) async { +    await SourceGroupCatalog.remove(groupName); +    for (final s in _sources) { +      if (s.bookSourceGroup.trim() == groupName) { +        await _dao.update(s.copyWith(bookSourceGroup: '')); +      } +    } +    _sources = await _dao.getAll(); +    notifyListeners(); +  } +    /// 浠?JSON 鏂囨湰鎴栦功婧愯闃?URL 瀵煎叆    Future<bool> importSources(String jsonText) async {      final text = _normalizeImportText(jsonText);      if (text.isEmpty) return false;   @@ -237,22 +287,100 @@ class SourceProvider extends ChangeNotifier {    /// 鎵归噺璁剧疆鍒嗙粍    Future<void> setSourcesGroup(      Iterable<String> sourceUrls,      String group,    ) async { -    final urls = sourceUrls.toSet(); -    if (urls.isEmpty) return; -    for (final url in urls) { -      BookSource? src; -      for (final s in _sources) { -        if (s.bookSourceUrl == url) { -          src = s; -          break; -        } -      } -      if (src == null) continue; -      await _dao.update(src.copyWith(bookSourceGroup: group)); +    await addGroupToSources(sourceUrls, group); +  } + +  /// 鎵归噺鍚敤/绂佺敤鍙戠幇 +  Future<void> setSourcesExploreEnabled( +    Iterable<String> urls, +    bool enabled, +  ) async { +    final urlSet = urls.toSet(); +    if (urlSet.isEmpty) return; +    for (final s in _sources) { +      if (!urlSet.contains(s.bookSourceUrl)) continue; +      await _dao.update(s.copyWith(enabledExplore: enabled)); +    } +    _sources = await _dao.getAll(); +    notifyListeners(); +  } + +  /// 灏嗛€変腑涔︽簮绉诲埌鍒楄〃椤堕儴 +  Future<void> moveSourcesToTop(Iterable<String> urls) async { +    final selected = urls.toSet(); +    if (selected.isEmpty) return; +    final orders = customOrdersAfterMoveToTop( +      sourcesInManualOrder(_sources), +      selected, +    ); +    await _applyCustomOrders(orders); +  } + +  /// 灏嗛€変腑涔︽簮绉诲埌鍒楄〃搴曢儴 +  Future<void> moveSourcesToBottom(Iterable<String> urls) async { +    final selected = urls.toSet(); +    if (selected.isEmpty) return; +    final orders = customOrdersAfterMoveToBottom( +      sourcesInManualOrder(_sources), +      selected, +    ); +    await _applyCustomOrders(orders); +  } + +  /// 鎸夌粰瀹?URL 椤哄簭閲嶆帓涔︽簮 +  Future<void> reorderSources(List<String> orderedUrls) async { +    if (orderedUrls.isEmpty) return; +    final orders = { +      for (var i = 0; i < orderedUrls.length; i++) orderedUrls[i]: i, +    }; +    await _applyCustomOrders(orders); +  } + +  /// 瀵煎嚭閫変腑涔︽簮涓?JSON 鏁扮粍 +  Future<String> exportSourcesJson(Iterable<String> urls) async { +    final urlSet = urls.toSet(); +    final list = _sources +        .where((s) => urlSet.contains(s.bookSourceUrl)) +        .map((s) => s.toJson()) +        .toList(); +    return jsonEncode(list); +  } + +  /// 涓洪€変腑涔︽簮璁剧疆鍒嗙粍 +  Future<void> addGroupToSources(Iterable<String> urls, String group) async { +    final urlSet = urls.toSet(); +    if (urlSet.isEmpty) return; +    for (final s in _sources) { +      if (!urlSet.contains(s.bookSourceUrl)) continue; +      await _dao.update(s.copyWith(bookSourceGroup: group)); +    } +    _sources = await _dao.getAll(); +    await SourceGroupCatalog.mergeFromSources([group]); +    notifyListeners(); +  } + +  /// 娓呴櫎閫変腑涔︽簮鐨勫垎缁?+  Future<void> clearGroupOnSources(Iterable<String> urls) async { +    final urlSet = urls.toSet(); +    if (urlSet.isEmpty) return; +    for (final s in _sources) { +      if (!urlSet.contains(s.bookSourceUrl)) continue; +      await _dao.update(s.copyWith(bookSourceGroup: '')); +    } +    _sources = await _dao.getAll(); +    notifyListeners(); +  } + +  Future<void> _applyCustomOrders(Map<String, int> orders) async { +    if (orders.isEmpty) return; +    for (final s in _sources) { +      final order = orders[s.bookSourceUrl]; +      if (order == null || order == s.customOrder) continue; +      await _dao.update(s.copyWith(customOrder: order));      }      _sources = await _dao.getAll();      notifyListeners();    }   diff --git a/test/models/book_source_engine_json_test.dart b/test/models/book_source_engine_json_test.dart index ae9aa73..48d7ad9 100644 --- a/test/models/book_source_engine_json_test.dart +++ b/test/models/book_source_engine_json_test.dart @@ -56,6 +56,38 @@ void main() {      };      final source = BookSource.fromJson(wrapper);      final engine = jsonDecode(source.toEngineJson()) as Map<String, dynamic>;      expect(engine['ruleContent'], isA<Map>());    }); + +  test('fromJson reads order and explore fields', () { +    final source = BookSource.fromJson({ +      'bookSourceUrl': 'https://a.example', +      'bookSourceName': 'A', +      'customOrder': 3, +      'lastUpdateTime': 100, +      'weight': 9, +      'enabledExplore': false, +      'respondTime': 5000, +      'exploreUrl': '[]', +    }); +    expect(source.customOrder, 3); +    expect(source.lastUpdateTime, 100); +    expect(source.weight, 9); +    expect(source.enabledExplore, false); +    expect(source.respondTime, 5000); +  }); + +  test('toEngineJson syncs order fields into raw map', () { +    final source = BookSource.fromJson({ +      'bookSourceUrl': 'https://a.example', +      'bookSourceName': 'A', +      'ruleContent': {'content': r'$.x'}, +      'customOrder': 1, +      'enabledExplore': false, +    }).copyWith(customOrder: 42, enabledExplore: true); +    final engine = jsonDecode(source.toEngineJson()) as Map<String, dynamic>; +    expect(engine['customOrder'], 42); +    expect(engine['enabledExplore'], true); +    expect(engine['ruleContent'], isA<Map>()); +  });  } diff --git a/test/pages/explore/explore_utils_test.dart b/test/pages/explore/explore_utils_test.dart new file mode 100644 index 0000000..01c4bc1 --- /dev/null +++ b/test/pages/explore/explore_utils_test.dart @@ -0,0 +1,31 @@ +import 'package:flutter_test/flutter_test.dart'; +import 'package:legado_flutter/models/book_source.dart'; +import 'package:legado_flutter/pages/explore/explore_utils.dart'; + +void main() { +  BookSource src({ +    bool enabled = true, +    bool enabledExplore = true, +    String exploreUrl = '[{"title":"a","url":"/"}]', +  }) { +    return BookSource.fromJson({ +      'bookSourceUrl': 'https://x', +      'bookSourceName': 'X', +      'enabled': enabled, +      'enabledExplore': enabledExplore, +      'exploreUrl': exploreUrl, +    }); +  } + +  test('manage explore_on ignores source.enabled', () { +    final s = src(enabled: false, enabledExplore: true); +    expect(hasExploreUrl(s), true); +    expect(isExploreEnabled(s), true); +    expect(sourceHasExplore(s), false); // tab still requires enabled +  }); + +  test('explore_off is hasUrl && !enabledExplore', () { +    final s = src(enabledExplore: false); +    expect(hasExploreUrl(s) && !isExploreEnabled(s), true); +  }); +} diff --git a/test/providers/source_order_logic_test.dart b/test/providers/source_order_logic_test.dart new file mode 100644 index 0000000..2d92a27 --- /dev/null +++ b/test/providers/source_order_logic_test.dart @@ -0,0 +1,57 @@ +import 'package:flutter_test/flutter_test.dart'; +import 'package:legado_flutter/models/book_source.dart'; +import 'package:legado_flutter/providers/source_order.dart'; + +void main() { +  test('move to top reindexes selected first', () { +    final all = [ +      BookSource(bookSourceUrl: 'a', bookSourceName: 'a', customOrder: 0), +      BookSource(bookSourceUrl: 'b', bookSourceName: 'b', customOrder: 1), +      BookSource(bookSourceUrl: 'c', bookSourceName: 'c', customOrder: 2), +    ]; +    final orders = customOrdersAfterMoveToTop(all, {'b'}); +    expect(orders['b'], 0); +    expect(orders['a'], 1); +    expect(orders['c'], 2); +  }); + +  test('move to bottom reindexes selected last', () { +    final all = [ +      BookSource(bookSourceUrl: 'a', bookSourceName: 'a', customOrder: 0), +      BookSource(bookSourceUrl: 'b', bookSourceName: 'b', customOrder: 1), +      BookSource(bookSourceUrl: 'c', bookSourceName: 'c', customOrder: 2), +    ]; +    final orders = customOrdersAfterMoveToBottom(all, {'b'}); +    expect(orders['a'], 0); +    expect(orders['c'], 1); +    expect(orders['b'], 2); +  }); + +  test('move to top uses customOrder when list is shuffled', () { +    final shuffled = [ +      BookSource(bookSourceUrl: 'c', bookSourceName: 'c', customOrder: 2), +      BookSource(bookSourceUrl: 'a', bookSourceName: 'a', customOrder: 0), +      BookSource(bookSourceUrl: 'b', bookSourceName: 'b', customOrder: 1), +    ]; +    final ordered = sourcesInManualOrder(shuffled); +    expect(ordered.map((s) => s.bookSourceUrl).toList(), ['a', 'b', 'c']); + +    final orders = customOrdersAfterMoveToTop(ordered, {'b'}); +    expect(orders['b'], 0); +    expect(orders['a'], 1); +    expect(orders['c'], 2); +  }); + +  test('move to bottom uses customOrder when list is shuffled', () { +    final shuffled = [ +      BookSource(bookSourceUrl: 'c', bookSourceName: 'c', customOrder: 2), +      BookSource(bookSourceUrl: 'a', bookSourceName: 'a', customOrder: 0), +      BookSource(bookSourceUrl: 'b', bookSourceName: 'b', customOrder: 1), +    ]; +    final ordered = sourcesInManualOrder(shuffled); +    final orders = customOrdersAfterMoveToBottom(ordered, {'b'}); +    expect(orders['a'], 0); +    expect(orders['c'], 1); +    expect(orders['b'], 2); +  }); +}
```
